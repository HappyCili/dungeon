# 聚宝之地刷取问题总结

> 更新日期：2026-07-23  
> 相关代码：`ui_app/treasure_farm.py`、`ui_app/tools/frida_hook_battle_info.js`  
> 配置表：`native_app/decrypted-data/`（mapareas / maplocations / zone-layouts）

---

## 1. 现象

刷取沉默之城（或其它聚宝图）时常见日志：

```text
开启普通宝箱：节点 6（钥匙 23）
开启普通宝箱暂不可用（已有地标激活 ret=2），稍后重试或重置地图
沉默之城 连续失败，尝试重置地图
聚宝之地重置被拒绝：随机结果异常（ret 3）
```

状态探针典型结果：

| 项 | 结果 |
| --- | --- |
| 所在 | 沉默之城（530101）· 聚宝图 |
| 阵亡 | 否 |
| 钥匙 / 炉温 | 正常（如钥匙 20+、炉温 200+） |
| 节点 | 大量 ACTIVE |
| 战斗协议 | **未收到** `Battle_info` / 战斗帧 |
| 登录战斗标识 | `battleState=0`，`battleType=7`（LOCEVT） |
| 地图运行态 | `area.locked=0`，`AreaDetail.mtdata` 不存在 |
| processloc 探针 | **ret=2**（已有地标激活） |
| 退出探针 | 常 **ret=3**（退不出） |
| 可执行 | **否** |

---

## 2. 结论（一句话）

**不是节点 6 的开箱协议写错。节点 6 已由 `maplocations` 确认为普通宝箱（`mapshow=2`、`monsterid=0`）。本次地标互斥的缺口在战后地图事件：怪物结算和钥匙物品展示会进入 `EventModule`，客户端完成“获得物品/对话”展示后发送 `Event_func_next(13325)`；旧脚本未处理该事件，服务端就持续保留地标占用。**

`battleType=7` 只说明登录快照中的地图事件上下文（LOCEVT），不是活动战斗标志；活动状态以 `battleState != 0` 为准。`Battle_info` 才含 battle_id、敌方站位等开战数据。没有它时不构造 `Battle_C2S_start`，而应继续处理地图事件状态机。

### 2.1 本次静态复核（2026-07-22）

已直接用解密客户端的 protobuf 生成器比对 `Client_talog`：

1. `params` 不是普通字符串 map，而是 `google.protobuf.Value` 的
   `structValue`，其中每个地图坐标/场景值是 `Value.number_value`（fixed64
   double）。旧脚本把数值写成字符串，且少了一层 `structValue` 包装，因此
   并不等价于官方客户端。
2. `treasure_farm.py` 已修正这段编码，并以客户端 `X2P` 输出的完整字节向量
   覆盖 `enter_map` 和 `scene_change`（包括旧场景为零的情形）。
3. `Game_data.field35.battle` 的 field3 / field4 分别是 `battleState` /
   `battleType`。客户端 `clientDataModule.onGameData` 保存它们，在
   `Client_data_get({keys:"*"})` 返回后发出
   `client_login_state({battleState:{state,type}})`。
4. `Client_data_get` 的消息号为 `10520`，生成器 `k6D` 对 `{keys:"*"}`
   的输出固定为 `0a 01 2a`。旧脚本未发送该登录期请求。
5. Stage 恢复顺序为 `scene_change(0, area)` → `enter_map(map,x,y)` →
   `Evt_script_trigger(type=2)`。`type=0` 属于回主城，不应在登录恢复时发送。
   `Map_movetrigger_active` 只在 `AreaDetail.mtdata.max > 0` 且移动触发条件满足时发送，
   不能作为 `ret=2` 的通用解除包。
6. `StageActor.CheckCannotMove` 在连续移动受阻后发送空载荷
   `Map_return_start(15560)`。脚本仅在 `battleState=0` 且没有任何战斗包时走这条
   原生地图回位路径；后续只由已选定的真实节点动作取得服务端交互结果。
7. `EventModule.OnEventFuncAction` 收到 `Event_func_action(13320)` 后，若等待类型为
   `ShowGetItem`、`ShowDialog` 或 `ItemInteraction`，在展示完成回调中调用
   `ClearFuncAction`，发送 `Event_func_next(13325)`。
8. `Event_func_next` 的常规对象是 `{skip: 0, pass: 0}`；protobuf 生成器对该对象编码为
   **空载荷**。这不是缺字段，而是客户端的正常完成确认。
9. `Event_start(13300)` 只有 `autoidx >= 100` 时才由客户端自动发送
   `Event_option(13305)`；其余选项保留给界面选择，脚本不得猜测分支。

脚本会先读取 `battleState/battleType`，完成 `10520` 往返，再按上述顺序请求恢复。
若服务端返回 `Battle_info`，立即用登录快照中的编队回发 `Battle_C2S_start` 并等待结算；
若未返回，保留精确的标识与收包状态，并停止节点操作。

---

## 3. 协议与表配置（native_app / 客户端）

### 3.1 开箱 / 点怪

- 消息：`Map_processloc`（15516）
- 请求体：`nodeid`（seat）field1 + `areaid` field2  
  （与客户端 `ZXO` 编解码、Stage 点地标一致）
- 成功：`ret=0` 且 `locchanges` 非空
- 中间态：`ret=0` 且空 `locchanges`（常随后开战）— **必须继续等，禁止当成功**
- **ret=2**：客户端 log「已经有地标激活」— **不结算节点**
- ret=60：需先 `Map_movetrigger_active` 再重试
- ret=7：死亡相关；ret=50：装备仓满

官方客户端开箱也是 **一次 processloc**（非战斗类型直接发；战斗类型可稍延迟），**没有**单独的「先交互、再开箱」第二条协议。

### 3.2 沉默之城表示例（mapareas）

| 字段 | 值 | 含义 |
| --- | --- | --- |
| id | 530101 | 区域 ID |
| name | 沉默之城 | |
| worldid | 2 | 聚宝 |
| mapgroup | 51 | 进图组 |
| iconshow | 1809 | 神秘钥匙·沉默 |
| costid | 空 | **进图不耗门票** |
| unlockticket | 15301 | 解锁条件（非每次进图门票） |

节点来自 `maplocations`：`nodeid` = seat；小宝箱如节点 6（mapshow=2），大宝箱如节点 33。  
布局坐标见 `zone-layouts/530101.json`（seat→x,y）。

### 3.3 大宝箱日限

- 客户端文案：`今日开启大宝箱次数 {{t}}/3`（`treasureopentimes2`）
- 脚本：`Map_treasure_info.times`，上限 `DAILY_BIG_CHEST_OPEN_LIMIT = 3`
- 达上限后本轮 `allow_big_chest=False`，只开普通箱/打怪

### 3.4 进图 / 重置

- `Map_enter_treasure`（15562）：`group` + `enterway`（Normal=0, Reset=1, FromCity=2）
- 聚宝重置应走 **enterway=Reset**，不要用魂域的 `Map_reset_area`（易莫名 ret）
- 地标锁死时 Reset 常见 **ret=3**（随机结果异常）— **重置解不了占用**

---

## 4. 为什么「游戏在战斗」而脚本「没有 Battle_info」

### 4.1 两套信号

| 信号 | 含义 | 自动化 |
| --- | --- | --- |
| processloc **ret=2** | 服务端地标/战斗占用 | **可靠**，会挡住一切点节点 |
| `Battle_info` **18002** | 开战握手（battle_id、敌方站位等） | 登录后**经常收不到** |
| `Game_data` field35 `battleState` | 登录恢复标识（field3） | **非零时执行恢复路径** |
| `Game_data` field35 `battleType` | 挂起战类型（field4） | 与 `battleState` 一起报告 |
| `Event_func_action` **13320** | 战后奖励展示/地图对话等待 | 收到已知无选择动作后确认 **13325** |
| `Event_end` **13315** | 地图事件链结束 | 清除本地事件等待状态 |

field35 不是 `Battle_info` 的替代品：它没有 battle_id、敌方站位和开战数据，不能直接构造
`Battle_C2S_start`；但它是脚本决定是否进入登录恢复的服务端标识。若 marker 为零且没有
18002，`ret=2` 的具体占用对象没有随公开协议返回，不能将其臆造为可结算战斗。

### 4.2 官方客户端链路（简图）

```text
Login / Game_data（field35: battleState / battleType，可能 curarea 已是聚宝图）
  → Client_data_get({keys:"*"}) → Client_data_get response
  → client_login_state({battleState:{state,type}})
  → PopSceneToStage 进入冒险 Stage
  → scene_change(0, area) → loadingBegin / 预加载 / RebuildStage / loadingFinished
  → enter_map(map,x,y) → Evt_script_trigger(type=2)
  → 服务端若存在可恢复战斗则补发 Battle_info(18002)
  → onBattleInfo → tryEnterBattle（若 isLoading 则等加载完）
  → Battle_C2S_start → 战斗帧 → Battle_S2C_end
  → Event_func_action(ShowGetItem / ShowDialog / ItemInteraction)
  → Event_func_next(skip=0, pass=0，空载荷) → Event_end
  → 地标释放后才能再 processloc 开箱
```

### 4.3 自动化实际链路

```text
WebSocket Login → Pack_password / Game_data
  → 解析 field35.battleState / battleType
  → `Client_data_get(10520, 0a012a)` 并等待同消息号响应
  → `scene_change` → `enter_map` → `Evt_script_trigger(type=2)`
  → 监听 `Battle_info` / `Battle_S2C_start` / 战斗帧
```

若 `battleState != 0` 且仍没有 18002，报告会明确给出 marker、10520 是否响应和所有
战斗消息号；脚本不会把该状态误判成空闲或继续盲点节点。

若 `battleState=0`（或 `Game_data` 无 battle blob）、10520 已响应且仍为 `ret=2`，
脚本先持续收取 `Event_start` / `Event_func_action` / `Event_end`。已知无选择展示会完成
`Event_func_next`；没有事件或仍未释放时才发送 `Map_return_start(15560)`。该请求没有
payload，来自客户端的 `StageActor.CheckCannotMove`，且不会在有战斗标识时执行。

### 4.5 本次会话的实测结果

完整状态探针（2026-07-22）返回：

```text
battleState=0 · battleType=7(LOCEVT) · Client_data_get=已响应
area(state=1, flag=0, locked=0, remains=2147483647) · mtdata=未激活
Battle_info/战斗帧=未收到 · processloc=ret2 · exit=ret3
```

这排除了构造 `Battle_C2S_start`（缺 battle_id 与战斗数据）和
`Map_movetrigger_active`（没有 `mtdata`）两条路径；同时补出了此前收包遗漏的
`Event_func_action → Event_func_next` 地图事件完成链。它与战斗启动不同：不需要
battle_id，不伪造战斗数据，只在服务端已下发对应等待动作后完成同一事件。

### 4.4 静态代码依据

| 项 | 结果 |
| --- | --- |
| 包名 | `com.zygames.dungeon4` |
| 传输 | `TJ.TJWebSocket`：`Send(Puerts.ArrayBuffer)` / `OnWebsocketMessage(byte[])` |
| 业务 | 主要在 **Puerts/JS**（main.js） |
| MsgHdr | field1 varint = message_id（与 `encode_message_header` 一致） |
| field35 | 生成器 `fr`：field3=`battleState`、field4=`battleType` |
| 登录分支 | `clientDataModule.onGameData`：保存 marker 后发送 `10520/k6D({keys:"*"})` |
| Stage 顺序 | `PopSceneToStage` 先 `scene_change`；`Stage.ReportEnterMap` 后 `enter_map`；完成后 type=2 |

这些结论直接来自 `decrypted-js/main.js`，协议编码由 `dragon_arena_static_proto.mjs`
加载同一份生成器复核；不依赖模拟器或运行时注入。

---

## 5. 历史错误流程（已修方向）

| 错误做法 | 后果 |
| --- | --- |
| 已在目标图时直接用登录 locs 开刷，不收敛战斗 | 带着占用开箱 |
| 钥匙够就优先开箱，忽略 ret=2 | 死循环「节点 6 ret=2」 |
| ret=2 连点多节点 / 强重置 | 仍失败，重置 ret=3 |
| 无探针结果也当「可执行」 | 假空闲 |
| `recover`/`finish_pending_battle` 误报成功 | 门禁被绕过 |
| 仅因无 Battle_info 判空闲 | 与官方 UI 矛盾 |
| 把 ret=5 一律当成缺门票 | 进聚宝 costid 为空；换图失败多因旧图退不出 |
| 将 `Map_processloc` 当作「状态探针」并点 ACTIVE 怪物 | `Map_processloc` 是正式地标动作，任务一启动就会主动进入战斗 |

---

## 6. 当前脚本行为（代码侧）

实现入口：`TreasureFarmClient` / `run_treasure_farm`（`treasure_farm.py`）。

1. **登录恢复标识**：解析 `Game_data.field35.battleState/battleType`，在状态报告中显示原始值与类型名。  
2. **官方登录请求**：收到 `Game_data` 立即发送 `Client_data_get(10520, 0a012a)`，并确认同消息号响应已到达。  
3. **进图后就绪**：严格按 `scene_change` → `enter_map` → `Evt_script_trigger(2)` 发送；不再错误发送 type=0。  
4. **挂起战**：若收到 18002，走 `finish_pending_battle`（开战/等结束）；field35 仅用于触发恢复，绝不伪造 18002。  
5. **战后事件完成**：处理 `Event_start`、`Event_func_action`、`Event_end`；对客户端定义的 `ShowGetItem`、`ShowDialog`、`ItemInteraction` 发送 `Event_func_next(13325)`，并记录“地图事件获得物品提示已确认”。普通选项不猜测。  
6. **地图残留恢复**：无 battle marker / 战斗帧且无可完成事件时，发送原生 `Map_return_start(15560)`；不再以 `Map_processloc` 重探。下一次已选定的节点动作负责取得实际结果。  
7. **非交互预检**：`ensure_actionable` 与 `verify_can_interact` 只处理已收到的 `Battle_info`、战斗帧和登录快照，绝不发送 `Map_processloc`。  
8. **移动触发器**：仅当 `mtdata.max > 0`、`remain=0` 时发送 `Map_movetrigger_active`；普通 `ret=2` 不发送。  
9. **大宝箱日限/开箱**：保留 times 日限；节点直接 `Map_processloc`，无需强制 `Map_move`。  

### 6.1 刷取节点阶段（重塑后）

刷取主循环不再把 `locchanges` 当作完整奖励结算，而是按节点类型等待对应的业务确认：

- **宝箱**：宝箱交互 → 打开宝箱（`locchanges`）→ 获得炉温奖励（炉温增量）。
- **怪物**：怪物交互 → 战斗准备（`skipTeam=false` 时记录）→ 进入战斗 →
  `Battle_S2C_end` 明确胜利 → 带走钥匙 → 获得地图钥匙奖励。

缺少炉温或钥匙增量时，本次动作不会增加开箱/击杀计数，也不会继续点击其它地标；
任务会保留当前阶段并报告未收到的奖励，避免重复交互造成 `ret=2`。
连续三次仍只有空 `locchanges` 时直接停止，不把节点本地标成已通过。

### 本次复核：任务一启动就进入战斗的原因

旧流程把 `Map_processloc(ACTIVE 怪物节点)` 放在 `ensure_actionable`、
`verify_can_interact` 和 `--status` 的「探针」中。该消息对应客户端
`OnMapProcessloc` 的真实点击，不是读取接口；所以即使当前钥匙足够、下一步本应
开宝箱，预检也会先点怪并创建战斗。

修正后的完整流程如下：

1. 登录，接收 `Game_data`，立即补 `Client_data_get(10520)`。
2. 进入目标聚宝图，接收 `Map_enter_area` 和节点 `locs`。
3. 发送 `scene_change → enter_map → Evt_script_trigger(2)`，仅收敛登录期已存在的战斗。
4. 读取钥匙和节点状态：钥匙足够时优先大宝箱、再普通宝箱；钥匙不足才选择怪物。
5. 对上述唯一选定的节点发送一次 `Map_processloc`；怪物节点才会产生正常战斗。
6. 怪物战后先完成服务端下发的地图事件：`Event_func_action → Event_func_next → Event_end`；随后读取物品和节点变更。
7. 若真实动作返回 `ret=2`，收集已有战斗握手和地图事件；仅在两者都没有可推进状态时执行 `Map_return_start`，不额外点击其他节点。
8. 以 `locchanges` 和物品 25（炉温）增量作为结算与完成依据。

状态查询：

```bash
cd ui_app
../venv/bin/python treasure_farm.py --zone-id 4101 --status
```

刷取（状态健康后）：

```bash
../venv/bin/python treasure_farm.py --zone-id 4101 --area-id 530101 --target-hearth 1
```

成功标准：farm 流程中 **炉温（物品 25）增量 > 0**，不能只用扫荡成功代替验收。

---

## 7. 纯脚本恢复输出

`--status` 会输出以下只读证据：

1. `battleState` / `battleType`：服务端 `Game_data` 的登录恢复标识。  
2. `Client_data_get=已响应/待响应`：10520 登录分支是否完整。  
3. `Battle_info`、`Battle_S2C_start`、战斗帧：是否取得足以代打的会话数据。  
4. `area(state/flag/locked/remains)` 与 `mtdata(max/remain/area/triggernum)`：客户端可见地图运行态。  
5. 节点状态统计与可交互怪物/宝箱数量：登录快照中的可选动作。  
6. `pending_event`：已收到但状态查询未确认的地图事件展示或自动选项。  

`--status` 不发送 `Map_processloc` 或 `Map_exit_area`，因此不会点击怪物、开箱或退出地图。`ret=2` 只来自一次已选定的真实节点操作；日志会保留该动作的节点和返回值。

状态输出始终打印 `battleState=<值>`，包括零值；不能从泛化的说明文案推断 marker 非零。

---

## 8. 代码能否单独解决？

| 问题 | 状态 |
| --- | --- |
| processloc 编码 / 节点 seat / 地图配置 | 已对齐 native 与客户端 |
| 开箱是否两步协议 | 否，一步 processloc；不是根因 |
| ret=2 下盲开箱死循环 | 已限制为真实节点动作后恢复，不再用其他节点重探 |
| 服务端登录恢复标识 | 已定位：`Game_data.field35.battleState/battleType` |
| 缺失的登录期请求 | 已补齐：`Client_data_get(10520, 0a012a)` |
| Stage 恢复上报顺序 | 已按静态客户端改为 `scene_change → enter_map → type=2` |
| 无战斗标识的地标残留 | 已补齐原生 `Map_return_start(15560)`；下一次真实节点动作确认结果 |
| 移动触发器误发 | 已修正：无 `mtdata` 时不发 `15574` |
| 收到 18002 后代打挂起战 | 已实现：`Battle_C2S_start` → 自动战斗 → 等结算 |
| 战后奖励/对话事件完成协议 | 已定位并实现：`Event_func_action(13320)` → `Event_func_next(13325, 空载荷)` → `Event_end(13315)` |

**结论：任务启动后自动进入战斗的预检问题已消除；节点 6 仍固定按普通宝箱处理。战后钥匙对话现由纯脚本完成官方事件确认，确认日志出现后再继续开箱。战斗只会来自已存在的服务器战斗，或钥匙不足时策略明确选中的怪物节点。**

---

## 9. 错误码速查

### Map_processloc（15516）

| ret | 含义 | 处理 |
| ---: | --- | --- |
| 0 + locchanges | 节点结算完成 | 正常 |
| 0 + 空 locchanges | 中间态，可能开战 | 继续等 Battle_info |
| 2 | 已有地标激活 | 保留本次节点动作结果，收集 battleState / Battle_info，执行恢复；勿点击其他节点作探针 |
| 7 | 死亡相关 | 复活 |
| 50 | 装备仓满 | 清仓 |
| 60 | 需 movetrigger | 发 15574 后重试 |

### Map_enter_treasure（15562）

| ret | 含义 |
| ---: | --- |
| 0 | 成功，随后 Map_enter_area |
| 5 | 道具/条件/也可能未解锁或退不出旧图 |
| 6 | 进入目标失败 |
| 7 | 已在当前组 → Reset 刷新 |
| 8 | 不在藏宝地无法重置 |

### Kickout（10030）

| ret | 含义 |
| ---: | --- |
| 2 | 顶号/会话切换 — 关游戏与其它自动化 |

---

## 10. 相关文件

| 路径 | 说明 |
| --- | --- |
| `ui_app/treasure_farm.py` | 刷取、只读状态、就绪序列、战斗恢复 |
| `ui_app/treasure_area.py` | 扫荡（与刷取不同，成功不能代替 farm 验收） |
| `ui_app/dragon_arena_static_proto.mjs` | 加载解密客户端生成器复核编码 |
| `.grok/skills/treasure-farm-troubleshoot/SKILL.md` | 排障 skill |
| `native_app/decrypted-data/tables/maplocations.json` | 节点 |
| `native_app/decrypted-data/mapareas.json` | 聚宝地图属性 |
| `native_app/decrypted-data/zone-layouts/` | seat 坐标 |

---

## 11. 验收顺序

1. 运行 `--status`，确认报告包含 `登录战斗标识` 和 `Client_data_get=已响应`，并确认没有任何节点交互日志。  
2. 若标识非零，确认登录战斗消息中出现 `Battle_info`，并由脚本自动进入结算。  
3. 启动刷取后，首条节点日志应与策略一致：钥匙足够时为开箱；钥匙不足时才是击杀怪物。  
4. 仅在该首个真实节点动作返回 `ret=2` 时进入恢复分支；验收日志中没有额外「探针」节点。  
5. 验收炉温（物品 25）实际增量大于零。
