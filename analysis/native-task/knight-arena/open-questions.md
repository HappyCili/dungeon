# 待确认项

| 问题 | 当前证据 | 结论边界 | 下一步判定规则 |
| --- | --- | --- | --- |
| `ma_arena_dailychallenge` 的配置来源 | 原生模块调用 `getNumber("ma_arena_dailychallenge")`；当前业务规则已确认每日免费挑战上限为 `5`，但同版本宏表仍未在工作区定位 | 值已明确；资源位置和后续版本是否变化仍未知 | 读取同版本宏表或运行时配置，确认升级版本仍使用 `5`；预算始终计算 `max(5-challengenum,0)`，不使用 `refreshnum` |
| `19814 Arena_get_dailyreward` 的请求体 | 找到响应 codec 和处理器，没有本地发送点或抓包帧 | 不构造/假设请求载荷 | 录制一次 UI 点击；若请求为空，仍需验证服务器返回 `ret` 和状态刷新 |
| 每日奖励的成功判定 | 原生 handler 未分支 `ret`，抓包无成功回包 | `getdailyreward=true` 的本地写入不单独证明服务器已发奖 | 将明确成功回包、后续 `Arena_info.getdailyreward=true` 或物品/排名奖励状态作为判定依据 |
| `18012.ret=7` 与 `19810.ret=9` 的错误语义 | 抓包可见数值；原生仅对挑战 `ret<=13` 选择具体本地化标签 | 错误码名称未知 | 提取同版本本地化表或补采 UI 提示文本；在此之前仅记录数值 |
| 成功战斗结算的完整时序 | 静态有 `19818` codec/处理器，抓包在 `18012.ret=7` 后中断 | 未验证 `18090`、物品变更、`19818` 的实际先后 | 采集一场 `18012.ret=0` 的完成战斗，逐帧记录 `18090`、`19818`、物品与积分推送 |
| `19806 Arena_set_attackteam` 与 `15016 Hero_setteam` 的实际 UI 发送选择 | 原生注册 `19806`，当前项目实现和测试使用通用 `15016`；抓包未见二者 | 不能断言每次原生 UI 都发 `19806` 或都发 `15016` | 在队伍编辑页分别保存、切换和直接挑战，记录消息序列 |
