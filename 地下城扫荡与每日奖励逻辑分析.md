# 地下城扫荡与每日奖励逻辑分析

## 结论摘要

项目里的“地下城每日奖励”实际包含两条独立链路：

| 链路 | 作用 | 核心消息 | 最终完成条件 |
| --- | --- | --- | --- |
| 地下城宝库 | 扫荡后消耗累计宝库次数并抽取掉落 | `19642 Dun_sweep` -> `19604 Dun_start_draw` -> `12602 Storage_item_change_notify` | 抽取响应 `ret=0`；库存同步以 source 71 通知为准 |
| 日常任务 107 | 完成任务 `50601`，增加 15 点日活并领取活跃奖励 | `19700 Dailyquest_info` -> `19702` -> `19704` | 日常状态从 `finished=false` 变为 `true`，领取状态在最终 `19700` 中确认 |

宝库抽取不是日活宝箱领取。地下城日常动作可以完成而不抽宝库，宝库抽取也不会自动等价于领取日活奖励。

## 一、地下城扫荡

### 1. 原生入口与前置条件

原生 `DungeonEnterPanel.OnClickWeep` 的实际顺序是：

1. 当前列表非空，目标在 `unlocks` 且 `showUnlocks` 中。
2. `compeleted[dungeon_id]` 为真，扫荡按钮才显示。
3. `challengeTimes[dungeon_id] < dungeon.limit`；普通地下城配置的 `limit` 为 3。
4. 消耗品满足 `dungeon.consume`；当前普通地下城表为 `901,1`。
5. 装备背包有空位。
6. 确认框展示历史最高分和按 `acquiringtime` 计算的宝库次数，确认后发送 `{dunid}`。

`DungeonSweepClient` 的状态投影来自 `Game_data` 的 field 18 `dungeon`：

```text
unlocks       -> 已解锁列表
showUnlocks   -> 面板可见列表
bests         -> 历史最高分
challengeTimes-> 各地下城挑战次数
drawtimes     -> 已抽取次数
totaltimes    -> 当前累计可抽次数
```

当前 Python 投影没有包含原生的 `compeleted`、`treasuryTimes`、`sweeps`，这是实现与原生前置条件之间的已知差异。

### 2. 扫荡协议

```text
message_id = 19642 (Dun_sweep)
request    = field 1: dunid:int32
response   = field 1: ret:int32
```

`ret=0` 才视为扫荡成功。原生 `zh-Hans.json` 已定义 `ret=1` 未通关、`2` 无积分记录、`3` 无扫荡次数、`5` 进地下城次数达到上限、`6` 材料不足、`999` 背包装备已满。自动任务对这些已证实的业务条件记录为跳过，不重试；未知 `ret` 仍作为失败保留。已有运行时记录中的 `ret=4` 尚无静态文案，不能自动解释或盲目重试。

### 3. 扫荡之后的宝库抽取

原生 UI 会在 `OnDunSweep(ret=0)` 发出 `dungeon_sweep_suc`，刷新面板；`TryDrawTreasure` 发现 `drawtimes < totaltimes` 时先发送 `19626 Dun_treasure_info {id}`，成功后打开 Normal/Nightmare 宝库面板。操作台的独立地下城任务为了完成闭环，直接发送 `19604`，不等待 19626。

```text
message_id = 19604 (Dun_start_draw)
request    = field 1: dunid:int32
             field 2: all:bool = true
response   = field 1: ret
             field 2: dunid
             field 3: drawtimes
             field 4: totaltimes
             field 5: dropids[]
             field 6: prob[]
             field 7: all
```

`DungeonSweepClient.draw_all` 在响应 ID 匹配且 `ret=0` 后，最多再等待 1 秒收集 `12602 Storage_item_change_notify`。只有 `source=71 (DUN_DRAW)` 的通知计入宝库奖励；没有通知时，界面可回退显示 `dropids/prob`，但不能宣称背包已经同步。

### 4. 运行时确认

52 个成功会话全部呈现：

```text
19642 request
  -> 19642 response ret=0
  -> 19604 request {dunid=2302, all=true}
  -> 19604 response {ret=0, all=true, drawtimes=3, totaltimes=3}
  -> 12602 source=71 (item + prop)
  -> 12602 source=71 (item + prop)
```

这证明了当前操作台“扫荡后全部抽取、再收集库存通知”的顺序和奖励落袋链路。

## 二、地下城日常任务与每日奖励

### 1. 任务配置

本地 `daily_quest.json` 的任务 107：

```text
id=107
groupId=1 (日常)
questid=50601
scoreday=15
scoreweek=0
```

`daily_actions.py` 将任务目标设为 1，但动作策略固定执行 3 次扫荡，优先选择服务端当前可见候选中历史最高分最高的地下城（同分按 ID 升序）。这是操作台策略，不是原生协议字段。

### 2. 日常状态与领取

1. 登录时从 `Game_data` field 19 读取 `dailyquest`，从 field 12 读取任务条件进度。
2. 主动发送 `19700 Dailyquest_info`，获取每日/每周剩余时间、已领取奖励 ID 和任务 `datas[]`。
3. 日常动作完成后再次查询；只有服务端把任务 107 标为 `finished=true` 才算完成。
4. 对每个 `finished=true && score_claimed=false` 的任务发送：

   ```text
   19702 Dailyquest_get_questreward
   field 1 = task id
   field 2 = group (1=日常, 2=周常)
   ```

5. 再次查询，按已领取任务的配置分数求 `activity_score`。对所有 `score <= activity_score` 且不在已领取列表的奖励逐个发送：

   ```text
   19704 Dailyquest_get_scorereward
   field 1 = group
   field 2 = activity reward id
   ```

6. 最后一次 `19700` 必须确认任务积分和日活奖励 ID 已进入服务端状态。

日常批次被取消时不会进入领取阶段，只刷新状态。

### 3. 与宝库奖励的边界

`run_dungeon_sweep_action` 只调用 `client.sweep()` 三次；它不调用 `draw_all()`。因此：

- 完成任务 107 后，日活积分领取走 `19702/19704`。
- 地下城宝库掉落领取走 `19604/12602`。
- 若需要两者都完成，必须在同一批次中显式编排地下城宝库任务和日常奖励任务，不能依赖某一个成功事件推断另一个已完成。

## 三、自动任务集成（2026-08-04）

自动任务列表新增两个可独立勾选的项目，固定在既有玩法之后按以下顺序执行：

1. `dungeon_sweep` 读取地下城页当前保存的 `dungeon_id`，复用 `DungeonService.run` 执行服务端状态校验、`19642 Dun_sweep`、`19604 Dun_start_draw(all=true)` 和奖励通知收集。未选择地下城和已证实的服务端业务拒绝（包括 `ret=3` 无扫荡次数）显示为跳过；未知拒绝码显示为失败。
2. `daily_rewards` 最后创建共享会话的 `DailyQuestClient`，调用 `claim_available`。它只领取服务端状态中 `finished=true && score_claimed=false` 的任务积分，以及达到阈值且未领取的日常/周常活跃奖励，不重复执行其他日常玩法动作。

这样，地下城扫荡若已推动任务 107 完成，随后同一自动批次的 `daily_rewards` 就能在 `19700 -> 19702 -> 19704` 闭环中领取可用积分和活跃奖励。两个任务各自保留服务端结果、奖励明细和跳过原因。

## 四、主要风险与待补证

1. Python 状态投影缺少原生 `compeleted`，可能在“已解锁但未通关”的目标上提前发送扫荡请求。
2. `ret=4` 的含义未知；现有实现应保留错误码，不应把它改写为资源不足或自动重试。
3. `Dun_point_update_notify`、`treasuryTimes` 的时序与作用尚未由本批 832 条地下城日志验证。
4. 日常日志验证了通用 `19702/19704` 协议，但未出现明确 `19702(id=107)` 出站帧。
5. 宝库面板 TextAsset 不在当前 AssetBundle 的可读 TextAsset 对象中，原生面板的 `all` 发送点仍属静态缺口。

详细事实表、消息字段、状态机、失败矩阵和验证记录见：

- [`analysis/native-task/dungeon-sweep/inventory.md`](analysis/native-task/dungeon-sweep/inventory.md)
- [`analysis/native-task/dungeon-sweep/message-map.md`](analysis/native-task/dungeon-sweep/message-map.md)
- [`analysis/native-task/dungeon-sweep/state-machine.md`](analysis/native-task/dungeon-sweep/state-machine.md)
- [`analysis/native-task/dungeon-sweep/failure-matrix.md`](analysis/native-task/dungeon-sweep/failure-matrix.md)
- [`analysis/native-task/dungeon-sweep/evidence.md`](analysis/native-task/dungeon-sweep/evidence.md)
- [`analysis/native-task/dungeon-sweep/verification.md`](analysis/native-task/dungeon-sweep/verification.md)
- [`analysis/native-task/dungeon-sweep/open-questions.md`](analysis/native-task/dungeon-sweep/open-questions.md)
