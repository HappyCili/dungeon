# 盈月之仪每日奖励接口分析

## 结论

“盈月之仪”在当前客户端中对应月度权益（`monthly_vip` / `MonthlyCard`）。每日奖励通过已登录游戏服的 WebSocket 业务消息领取，而不是独立 HTTP 接口。

| 项目 | 内容 |
| --- | --- |
| 业务消息 | `Pay_get_monthly_vip_dailyrewards` |
| 消息号 | `12416` |
| C2S 编码 | `kfP` / `eb` |
| C2S 字段 | `id`，字段 #1，`int32` |
| S2C 解码 | `skJ` / `tb` |
| S2C 字段 | `id`（#1，`int32`）、`result`（#10，`int32`） |
| 基础权益 ID | `1` |
| 进阶权益 ID | `2` |
| 成功结果 | `result=0` |
| 已领取结果 | `result=3` |
| 奖励通知 | `Storage_notify_itemchange`（`12602`），来源 `MONTHLY_VIP_DAILY=142` |

客户端会遍历 `payInfo.privileges.vipInfo.VIPs`：当对应权益未在当天领取、且本地的月度权益天数道具大于零时，发送下列对象：

```text
{ id: <1 或 2> }
```

基础和进阶权益天数分别由物品 ID `120` 与 `121` 表示。是否已购买、是否可领取、是否重复领取均由服务端响应决定。

## 调用链

```text
tokens.json 的 userid + verify_token
  -> 账号服 Logincheck
  -> 区服网关
  -> 游戏 WebSocket Login / Pack_password / Login_reunique
  -> Pay_get_monthly_vip_dailyrewards (12416, { id })
  -> 12416 响应 ({ id, result })
  -> 可选的 Storage_notify_itemchange (source=142)
```

业务包沿用项目现有游戏服封装：Protobuf `MsgHdr`、Pack1、DES-ECB/PKCS#7 和 Base64 WebSocket 文本帧。令牌只用于建立会话，不写入请求业务体。

## 脚本

`claim_monthly_vip_daily_rewards.py` 复用 `harvest_fief.py` 的登录、WebSocket 和 Pack1 实现：

```bash
venv/bin/python claim_monthly_vip_daily_rewards.py
venv/bin/python claim_monthly_vip_daily_rewards.py --vip-id 1
venv/bin/python claim_monthly_vip_daily_rewards.py --self-test
```

默认按客户端逻辑顺序请求基础权益（`1`）和进阶权益（`2`），不会输出令牌值。脚本将响应 `result` 与来源为 `142` 的物品变更一并输出，用于确认实际请求结果。
