# 秘法塔每日免费探索接口分析

## 结论

秘法塔的探索复用通用抽取协议。每日免费额度由登录同步的 `Game_data.banners`
下发，调用前应以服务端状态为准，只处理秘宝分类且 `freePullTimesLeft > 0` 的池。

| 项目 | 内容 |
| --- | --- |
| 登录同步 | `Game_data` (`10490`) |
| 探索池列表 | `Game_data.banners`，字段 #21，重复消息 |
| 秘法塔分类 | `GACHA_CATEGORY_ARTIFACT = 1` |
| 池 ID | `bannerId`，嵌套字段 #1 |
| 今日已探索 | `pullTimesToday`，嵌套字段 #2 |
| 免费剩余次数 | `freePullTimesLeft`，嵌套字段 #3 |
| 免费刷新倒计时 | `refreshFreeLeftSecs`，嵌套字段 #4 |
| 探索消息 | `Pull_gacha_banner_v2` (`19532`) |
| 成功结果 | `result = 0` |
| 免费额度已使用 | `result = 3` |

`Pull_gacha_banner_v2` 的免费单次请求字段如下：

```text
bannerId = <Game_data 返回的池 ID>  (#1, int32)
pullTimes = 1                       (#2, int32)
category = 1                        (#3, int32)
freePull = true                     (#5, bool)
costItemId = omitted                (#6, optional int32)
```

显式设置 `freePull=true` 且省略 `costItemId`，使请求只走服务端的免费额度校验。
脚本不会在免费额度为零时改用渡鸦之羽或其他资源。

## 执行流程

```text
tokens.json 的 userid + verify_token
  -> 账号服 Logincheck
  -> 区服网关
  -> 游戏 WebSocket Login / Pack_password
  -> Game_data(10490) 读取 banners
  -> 等待 Login_reunique
  -> 筛选 category=1 且 freePullTimesLeft>0
  -> Pull_gacha_banner_v2(19532)，单次并显式免费
  -> 校验 result、池 ID、分类与更新后的免费次数
  -> 输出响应 props 奖励并追加 JSONL 结果日志
```

每个池的实际调用次数以登录时下发的免费剩余次数为上限。若响应返回新的池状态且
免费次数已经归零，则立即停止该池后续调用；若服务端在并发状态变化后返回
`result=3`，同样停止该池。

## 脚本

直接检查并使用当前账号的免费额度：

```bash
../.venv/bin/python explore_arcane_tower_daily_free.py
```

指定区服：

```bash
../.venv/bin/python explore_arcane_tower_daily_free.py --zone-id 4101
```

只做本地协议与模拟会话校验：

```bash
../.venv/bin/python explore_arcane_tower_daily_free.py --self-test
```

脚本不会输出本地令牌、临时游戏 token 或 WebSocket 会话密码。

## 结果日志

脚本默认把每次检查与探索响应追加到 `arcane_tower_daily_free.jsonl`。成功探索记录
包含池 ID、服务端结果码、免费额度变化，以及每项奖励的 `kind`、`id` 和
`amount`；没有免费额度时记录 `request_count=0`。日志不包含登录令牌、临时游戏
token、WebSocket 地址或会话密码。

可通过参数指定其他日志文件：

```bash
../.venv/bin/python explore_arcane_tower_daily_free.py --result-log logs/arcane.jsonl
```
