# 每日任务功能清单与实施计划

更新日期：2026-07-19

## 1. 结论

从本地游戏配置中确认：`daily_quest` 共 31 条，其中 `groupId=1`、`scoreday>0` 的正式日常任务 20 条；`groupId=2` 的 11 条是周常任务，不计入本清单。

按“仓库中是否已有可执行代码完成该任务要求的核心动作”统计：

| 分类 | 数量 | 任务 |
| --- | ---: | --- |
| 已完成 | 3 | 秘法塔探索 1 次、龙痕竞技场胜利 1 次、古律院铭刻 1 次 |
| 已实现动作，待日常闭环 | 1 | 冒险者公会“换一批”5 次 |
| 部分完成，计入未完成 | 1 | 庄园普通收取 2 次 |
| 未实现 | 15 | 其余任务 |
| 二元汇总 | 20 | 已完成 3，未完成 17（含 2 项待闭环/部分完成） |

当前已完成动作理论覆盖 30 点日常活跃度，达到第一个 20 点奖励档位。实际是否已完成、是否已领取，必须以账号当日 `Dailyquest_info` 状态为准。

当前没有统一的日常状态查询、任务积分领取、活跃奖励领取和每日编排入口。因此，“已完成”仅表示对应任务动作已有完整可执行实现，不表示整套每日任务自动化已经闭环。

首版交付物必须包含一个本地可视化操作台，而不是只提供命令行脚本。操作台统一配置账号、密码和区服，并提供“日常任务”与“龙痕竞技场”两个功能页；前者按清单勾选自动执行项，后者配置刷取轮数以及“仁慈/处决”胜利抉择。

## 2. 判定口径

- **已完成**：已有与任务动作一致的可执行脚本，目标次数无已知缺口，且本地协议/模拟会话自检通过。
- **已实现动作，待日常闭环**：核心业务请求已实现，但策略可能提前停止，或者尚未按服务端任务剩余进度补足次数。
- **部分完成**：已有相关动作，但次数、动作变体或完成状态确认尚未覆盖；在二元汇总中按未完成处理。
- **未实现**：只有客户端静态代码、消息常量或分析材料，或者没有对应业务脚本。仅定位协议不算功能完成。
- **日常闭环**：动作完成后还需查询服务端任务状态、领取任务积分并领取达到阈值的活跃奖励。该共性能力目前尚未实现。

## 3. 清单来源与完整性

| 数据 | 用途 |
| --- | --- |
| [`decrypted-task-data/daily_quest.json`](../native_app/decrypted-task-data/daily_quest.json) | 日常/周常任务 ID、任务引用、活跃度、解锁票据 |
| [`decrypted-task-data/quests.json`](../native_app/decrypted-task-data/quests.json) | 任务名称、描述、跳转入口、完成条件引用 |
| [`decrypted-task-data/questfinishcond.json`](../native_app/decrypted-task-data/questfinishcond.json) | 条件类型、目标值和条件文本 |
| [`decrypted-task-data/activityreward.json`](../native_app/decrypted-task-data/activityreward.json) | 日常/周常活跃奖励阈值和奖励箱 ID |
| [`decrypted-data/rewardbox.json`](../native_app/decrypted-data/rewardbox.json) | 活跃奖励箱的实际奖励内容 |
| [`decrypted-task-data/manifest.json`](../native_app/decrypted-task-data/manifest.json) | 解密来源、文件大小和 SHA-256 |

已执行结构校验：31 个任务 ID 和 31 个 `questid` 均唯一；20 条日常、11 条周常均能唯一关联到 `quests` 和 `questfinishcond`；7 个解密文件的 SHA-256 均与 manifest 一致。日常任务总活跃度为 245，周常任务总活跃度为 150。

## 4. 正式每日任务清单

“解锁票据”为配置中的 `unlockticket` 原值；`-` 表示没有额外票据限制。优先级表示后续实现顺序，已完成项的 P0 是接入统一日常闭环。

| 日常 ID | 任务 ID | 任务条件 | 活跃度 | 解锁票据 | 当前状态 | 证据或缺口 | 优先级 |
| ---: | ---: | --- | ---: | ---: | --- | --- | --- |
| 101 | 50001 | 在冒险者公会使用 5 次“换一批” | 15 | - | **已实现动作，待日常闭环** | [`adventurer_guild_daily_auto_refresh.py`](adventurer_guild_daily_auto_refresh.py) 已实现受限自动刷新且自检通过；遇到 SS 或策略上限会提前停止，尚未按日常剩余进度确保补足 5 次 | P0 |
| 102 | 50101 | 在任意商店购买 1 次商品 | 20 | - | 未实现 | 未发现商店选品、预算限制和购买脚本 | P2 |
| 103 | 50201 | 在铁匠铺完成 5 次锻造 | 20 | - | 未实现 | 未发现普通锻造脚本 | P2 |
| 104 | 50301 | 在秘法塔完成 1 次探索 | 10 | - | **已完成** | [`explore_arcane_tower_daily_free.py`](explore_arcane_tower_daily_free.py) 实现免费探索、状态筛选和结果校验；本地自检通过 | P0 集成 |
| 105 | 50401 | 在庄园普通收取 2 次资源 | 10 | - | **部分完成** | [`harvest_fief.py`](harvest_fief.py) 已真实成功完成 1 次普通收取，但单次运行只发送 1 次请求，缺少剩余次数计算和任务状态确认 | P0 |
| 106 | 50501 | 在庄园快速收取 1 次资源 | 10 | - | 未实现 | 现有庄园脚本固定为普通收取，不满足快速收取条件 | P1 |
| 107 | 50601 | 完成 1 次地下城挑战 | 15 | - | 未实现 | 未发现地下城进入、战斗和结算脚本 | P3 |
| 108 | 50701 | 在公会委托中派遣 3 次 | 15 | - | 未实现 | 未发现委托选择、派遣和次数确认脚本 | P3 |
| 109 | 50801 | 在骑士比武中完成 3 次挑战 | 15 | - | 未实现 | 龙痕竞技场脚本属于不同玩法；普通竞技场 `Arena_*` 链路未实现 | P3 |
| 110 | 50901 | 在聚宝之地开启 1 次大宝箱 | 15 | - | 未实现 | 未发现聚宝之地进入、开箱和资源检查脚本 | P3 |
| 111 | 51001 | 在联盟完成 2 次捐献 | 10 | 12001 | 未实现 | 客户端存在公会捐献消息，但仓库没有可执行捐献客户端 | P2 |
| 112 | 51101 | 在龙痕竞技场挑战胜利 1 次 | 10 | 44 | **已完成** | [`dragon_arena.py`](dragon_arena.py) 实现匹配、挑战、战斗握手、胜利结算和抉择；真实日志已记录胜利；本地自检通过 | P0 集成 |
| 113 | 51201 | 在裂境角逐完成 1 次挑战 | 10 | 14006 | 未实现 | 未发现裂境角逐挑战和结算脚本 | P3 |
| 114 | 51301 | 在审判庭完成 1 次人物送礼 | 10 | - | 未实现 | 未发现礼物选择、库存检查和送礼脚本 | P2 |
| 115 | 51401 | 在铁匠铺完成 1 次精炼 | 10 | 7013 | 未实现 | 未发现精炼材料选择和执行脚本 | P2 |
| 116 | 51501 | 领取 1 次军团税收 | 10 | 13 | 未实现 | 未发现军团税收状态查询和领取脚本 | P1 |
| 117 | 51601 | 进入 1 次阴魂大厅 | 10 | 1300001 | 未实现 | 未发现大厅进入消息和进入成功确认 | P1 |
| 118 | 51701 | 完成 1 次无序迷境挑战 | 10 | 47 | 未实现 | 未发现无序迷境队伍、战斗和结算脚本 | P3 |
| 119 | 51801 | 在古律院完成 1 次铭刻 | 10 | 13003 | **已完成** | [`engrave_ancient_law_court_daily_free.py`](engrave_ancient_law_court_daily_free.py) 已实现免费次数检查、铭刻与成本校验；真实日志记录 1 次成功；本地自检通过 | P0 集成 |
| 120 | 51901 | 在铁匠铺完成 1 次高温锻造 | 10 | 9302 | 未实现 | [`harvest_furnace.py`](harvest_furnace.py) 是“萃华仪收获”，不是铁匠铺高温锻造，不能复用为完成证据 | P2 |

## 5. 日常活跃奖励

| 活跃度 | 奖励箱 ID | 配置奖励 |
| ---: | ---: | --- |
| 20 | 300101 | 金币 100、魔晶 300 |
| 40 | 300102 | 金币 150、魔晶 300 |
| 60 | 300103 | 金币 200、魔晶 300 |
| 80 | 300104 | 金币 250、魔晶 300 |
| 100 | 300105 | 铸宫探秘宝箱 1、魔晶 300 |

客户端协议显示，日常状态来自登录 `Game_data.dailyquest` 或 `Dailyquest_info` (`19700`)。完成任务后的积分领取使用 `Dailyquest_get_questreward` (`19702`)，活跃奖励领取使用 `Dailyquest_get_scorereward` (`19704`)。`daily_quest.py` 已实现这三个接口，并提供 `status`、`claim` 和脱敏 JSONL 结果记录；真实只读查询记录仍需在实际区服上留存并与游戏界面对照。

## 6. 已有但不属于正式日常任务的功能

| 功能 | 当前情况 | 不计入原因 |
| --- | --- | --- |
| 盈月之仪每日奖励 | [`claim_monthly_vip_daily_rewards.py`](claim_monthly_vip_daily_rewards.py) 可执行且自检通过 | 属于月度权益每日奖励，不在 `daily_quest` 的 20 条日常任务中 |
| 萃华仪收获 | [`harvest_furnace.py`](harvest_furnace.py) 可执行、已有真实成功记录且自检通过 | 不等于普通锻造、精炼或高温锻造 |
| 活动签到 | [`活动签到接口分析.md`](活动签到接口分析.md) 已定位协议 | 当前只有静态分析，没有独立可执行签到脚本；且活动签到不在本任务表中 |
| 登录与游戏服会话 | [`login.py`](login.py) 和 [`harvest_fief.py`](harvest_fief.py) 已实现 | 是所有任务的公共基础设施，不是日常任务动作 |

## 7. 应用框架与界面方案

### 7.1 技术选型

采用本机运行的单进程 Web 应用，默认只监听 `127.0.0.1`。页面由浏览器打开，后端直接导入现有 Python 客户端类，不通过 shell 拼接命令调用脚本。

| 层级 | 选型 | 职责 |
| --- | --- | --- |
| 后端 Web | Flask | 页面、JSON API、会话和错误处理 |
| 页面模板 | Jinja2 | 生成首屏和静态资源版本号 |
| 前端 | 原生 JavaScript + CSS | 表单、任务勾选、分段选择、进度轮询和运行日志 |
| 后台任务 | `ThreadPoolExecutor(max_workers=1)` + 线程安全事件队列 | 串行执行耗时游戏操作、进度上报和协作式停止 |
| 配置 | `dataclasses` + JSON 原子写入 | 保存账号名、区服、任务选择和竞技场选项 |
| 密码 | 系统凭据库（Python `keyring`） | 可选保存密码；配置文件中不写明文密码 |
| 业务层 | 现有 Python Client 类 + 统一适配器 | 登录、日常状态、任务动作和竞技场循环 |

计划新增并固定兼容版本的依赖为 `Flask` 和 `keyring`；前端不引入 React、Vue、Electron 或 Node 构建链。开发入口建议为：

```bash
../.venv/bin/python ui_app.py --host 127.0.0.1 --port 8765
```

应用启动后直接显示操作台，不增加介绍页或营销页。

### 7.2 页面结构

页面采用紧凑的工作台布局：顶部为账号和区服配置，主体为两个标签页，底部为当前任务进度与脱敏日志。页面区块不嵌套装饰性卡片。

首版页面草图：

```text
+------------------------------------------------------------------+
| 账号 [________________]  密码 [************]  [登录并获取区服]   |
| 区服 [请选择区服 v]                         状态：未连接          |
+------------------------------------------------------------------+
| [日常任务] [龙痕竞技场]                                         |
+------------------------------------------------------------------+
| 日常任务                                                         |
| [ ] 101  冒险者公会换一批 5 次     0/5   可执行                 |
| [x] 104  秘法塔探索 1 次           0/1   可执行                 |
| [ ] 102  任意商店购买 1 次         0/1   尚未接入               |
|                         [选择可执行项] [执行已选] [停止]          |
+------------------------------------------------------------------+
| 竞技场：轮数 [ 10  -/+]  抉择 [仁慈 | 处决]  [开始刷取] [停止]   |
+------------------------------------------------------------------+
| 进度：空闲  |  脱敏运行日志                                      |
+------------------------------------------------------------------+
```

桌面端保持上述横向布局；窄屏下账号、区服和命令区按字段换行，任务表允许横向滚动，但任务名、进度和执行结果不能互相遮挡。

#### 账号与区服

- 账号输入框。
- 密码输入框，默认遮罩；使用眼睛图标切换显示，并提供“记住密码”复选框。
- “登录并获取区服”命令按钮。
- 区服下拉菜单，在登录成功并取得区服列表后启用；显示区服名，内部保存原始 `zid`。
- 连接状态只显示未配置、登录中、可用、失败四类状态，不回显密码、令牌或游戏服地址。
- 非敏感设置变更后原子保存；密码默认只保留在进程内，勾选“记住密码”时才写入系统凭据库。

登录流程复用 `login.py` 的 `GameLoginClient` 和 `TokenStore`。区服获取逻辑从 `resolve_game_endpoint()` 中拆出 `list_zones()`，使界面先展示区服列表，再按选择的 `zid` 获取游戏服入口。

#### 日常任务标签页

- 顶部显示当日活跃度、下一个奖励阈值、已完成数和刷新倒计时。
- 主体为 20 行固定任务表，列为：勾选框、日常 ID、任务名、目标、当前进度、活跃度、实现状态、执行结果。
- 已有自动化适配器的任务可勾选；尚未实现的任务禁用勾选框，并通过悬停提示显示缺口。
- 提供“选择可执行项”“清空选择”“刷新状态”“执行已选”“停止”命令。
- 勾选结果按账号和区服保存；重新登录或切换区服后加载对应选择。
- 执行时逐行更新等待、运行、完成、跳过、失败状态；已完成任务不再次执行。
- 当前可优先接入任务 101、104、105、112、119，其中 101、105 必须先补齐次数和状态确认逻辑。

#### 龙痕竞技场标签页

- 轮数使用数字步进器，范围 `1..100`，默认 10；后端再次校验范围。
- 胜利抉择使用“仁慈 / 处决”分段控件，只允许二选一。
- “寻找对手耗尽后自动刷新”使用开关，默认开启，对应现有 `refresh_on_exhaustion`。
- 提供“开始刷取”和“停止”命令。
- 运行区显示目标轮数、已处理轮数、胜/负场数、当前阶段、积分变化、龙痕币变化和最后一条结果。
- 登录时检测到遗留战斗，先用本次选择的胜利抉择恢复结算，再开始新一轮。

现有代码只验证了仁慈 `choiceid=2`。界面接入处决前，必须从 `scararena_winchoice` 配置或真实客户端请求确认处决 ID，并以 `OutcomeMode -> choice_id` 映射保存；页面不得直接传任意数字。`dragon_arena.py` 需要把 `mercy_choice_id`、`choose_mercy()` 和硬编码“仁慈”日志重构为通用的 `outcome_mode`、`win_choice_id` 和 `choose_outcome()`，同时保留原 CLI 参数的兼容入口。

### 7.3 配置模型

非敏感配置建议保存到 `config/ui-settings.json`，使用临时文件加 `os.replace()` 原子更新，文件权限设为 `0600`：

```json
{
  "version": 1,
  "account": {
    "username": "",
    "remember_password": false
  },
  "zone": {
    "id": "",
    "name": ""
  },
  "daily": {
    "enabled_task_ids": [104, 112, 119]
  },
  "arena": {
    "rounds": 10,
    "outcome": "mercy",
    "refresh_on_exhaustion": true
  }
}
```

- JSON 中不得出现 `password`、`id_token`、`verify_token`、`pay_token`、游戏 token 或会话密码。
- 系统凭据库的键使用“应用名 + 账号”，API 只返回 `password_configured` 布尔值。
- `tokens.json` 继续由 `TokenStore` 管理并保持 `0600`；浏览器 API 不提供读取令牌的接口。
- 日常勾选配置最终按“账号 + 区服”隔离；首版可只维护一个当前配置，切换时先保存旧配置再加载新配置。

### 7.4 后端模块边界

建议目录结构：

```text
ui_app.py
requirements.txt
app/
  __init__.py
  config_store.py
  credentials.py
  job_manager.py
  models.py
  routes.py
  services/
    account_service.py
    daily_service.py
    arena_service.py
  templates/
    index.html
  static/
    app.js
    app.css
```

业务层统一返回结构化结果，不把终端文本作为页面数据源：

```text
TaskRunResult(task_id, status, progress_before, progress_after, message, rewards)
ArenaRunResult(requested_rounds, completed_rounds, wins, losses, score_delta, rewards)
JobEvent(sequence, timestamp, level, feature, message, data)
```

核心 API：

| 方法与路径 | 用途 |
| --- | --- |
| `GET /api/config` | 返回脱敏配置和可用能力 |
| `POST /api/account/login` | 使用提交的账号密码登录并返回区服列表 |
| `PUT /api/config/zone` | 保存当前区服 |
| `GET /api/daily-tasks` | 返回 20 条任务、服务端进度和是否可勾选 |
| `PUT /api/daily-tasks/selection` | 保存勾选的任务 ID |
| `POST /api/jobs/daily` | 启动已选日常任务 |
| `POST /api/jobs/arena` | 按轮数和胜利抉择启动竞技场 |
| `GET /api/jobs/{job_id}` | 轮询状态、进度和增量事件 |
| `POST /api/jobs/{job_id}/cancel` | 请求在当前原子操作结束后停止 |

所有修改类 API 只接受 JSON、检查同源请求并校验字段类型与取值范围。服务默认不启用 CORS，不监听局域网地址；同一时间只允许一个游戏任务运行，冲突请求返回 `409`。

### 7.5 运行状态与停止语义

- 作业状态固定为 `queued -> running -> stopping -> succeeded/failed/cancelled`。
- “停止”是协作式停止：不在 WebSocket 帧处理或战斗结算中间强行终止，只在一次任务动作或一轮竞技场结束后退出。
- 日常任务和龙痕竞技场共享同一个作业锁，避免同账号并发登录导致 `Kickout ret=2`。
- 页面刷新后通过当前作业 ID 恢复显示，不重复启动作业。
- 关闭浏览器不停止后台作业；关闭服务进程时等待当前客户端关闭 WebSocket，并把未完成作业标记为中断。

### 7.6 界面验收标准

1. 能在界面输入账号和密码、登录、获取并选择区服；失败时显示可操作的错误状态。
2. 密码框默认遮罩，配置和 API 响应中均无明文密码或令牌。
3. 日常页完整显示 20 条任务，只有已接入适配器的任务可勾选，保存后重启仍能恢复选择。
4. 执行日常时，页面逐项显示进度和结果，已完成任务不会重复执行。
5. 竞技场轮数和仁慈/处决能保存、提交并映射到经过验证的 `choice_id`；非法轮数和未知抉择被拒绝。
6. 两个功能不能并发运行；停止、页面刷新和服务退出均保持一致的作业状态。
7. 使用 Flask 测试客户端覆盖全部 API、配置迁移、凭据接口替身、并发冲突和错误响应。
8. 使用 Playwright 在 `1280x800` 和 `390x844` 检查首屏、两个标签页、运行态与错误态；任务表、按钮和文本不得重叠或溢出。

### 7.7 首版范围边界

- 首版只有一个页面、两个功能标签和一个串行作业队列，不增加用户管理、远程部署、多账号并发、统计报表或插件系统。
- 日常任务表始终展示配置中的全部 20 条正式日常；已有动作适配器的任务可勾选，尚未接入的任务置灰。后续接入适配器后自动开放对应勾选框，不改页面结构。
- 竞技场首版只接受轮数与“仁慈/处决”两类业务选项；队伍编排、对手筛选、资源预算等高级策略不进入首版。
- 页面不直接持有 WebSocket、游戏 token 或协议载荷；所有游戏通信均由后端业务层完成。
- 界面服务只监听本机，并在浏览器关闭后保留当前作业状态；服务进程重启后不自动恢复未完成的消耗型操作。

## 8. 实施计划

### 阶段 0：框架骨架与静态界面（P0）

1. 按 7.4 的目录结构建立 Flask 应用、配置模型、两个标签页和响应式布局。
2. 使用固定夹具渲染 20 条日常任务，并完成可执行/未接入两类勾选状态。
3. 完成账号、密码、区服、轮数和仁慈/处决控件；先接模拟服务，不发起真实游戏请求。
4. 建立单作业状态机、事件轮询和停止按钮，使用模拟耗时任务验证页面状态流转。
5. 加入 Flask API 测试和桌面/移动端 Playwright 截图检查。

验收：在没有账号和网络的情况下也能启动界面并完整演示配置、勾选、竞技场参数、运行进度和停止流程；页面任何状态均不显示密码或令牌。阶段 0 完成后，后续阶段只替换业务适配器，不重做界面框架。

### 阶段 A：日常状态与奖励闭环（P0）

1. ~~新增 `daily_quest.py`，复用 `harvest_fief.py` 的登录、WebSocket、Pack1 和 Protobuf 基础能力。~~（已完成）
2. ~~解码 `Game_data` 字段 19 的 `dailyquest`，并支持主动查询 `Dailyquest_info` (`19700`)。~~（已完成）
3. ~~输出 20 条任务的 `finished`、`getscore`、当前进度、日常剩余时间和已领取活跃奖励。~~（已完成）
4. ~~实现 `Dailyquest_get_questreward` (`19702`) 和 `Dailyquest_get_scorereward` (`19704`)；仅领取服务端明确可领取的项目。~~（已完成）
5. ~~增加 `status`、`claim` 命令和脱敏 JSONL 日志。~~（已完成）

阶段状态：实施项 1 至 5 已完成；真实只读查询记录仍待在实际区服留存并与游戏界面对照，因此阶段验收尚未完成。

验收：模拟会话覆盖未完成、已完成未领分、已领分、达到多个奖励档、重复领取和跨日刷新；真实只读查询能与游戏界面一致；重复执行 `claim` 不产生额外消耗或错误状态。

### 阶段 B：接入现有动作（P0）

1. ~~将冒险者公会、秘法塔、龙痕竞技场和古律院封装为可由统一入口调用的动作函数。~~（已完成）
2. 为庄园普通收取增加“按服务端剩余进度执行”的次数控制，最多补足到 2 次。
3. ~~为冒险者公会增加“按服务端剩余进度补足到 5 次”的日常模式；保留发现 SS 时停止的保护并明确报告任务未完成。~~（已完成）
4. ~~每个动作前后调用日常状态查询；只有服务端从未完成变为完成才记录成功。~~（已完成）
5. ~~动作完成后统一领取任务积分，再领取新达到的活跃奖励。~~（已完成）

阶段状态：实施项 1、3 至 5 已完成；第 2 项尚未完成。普通收取成功后当前协议约束要求等待新的资源产出，现有编排每次作业只执行 1 次，尚不满足“最多补足到 2 次”的原始目标，因此阶段验收尚未完成。

验收：任务 101、104、105、112、119 分别在测试夹具中完成闭环；策略停止、没有免费次数、没有可收取资源、竞技场失败时均保留准确的未完成状态；第二次运行不重复执行已完成动作。

### 阶段 C：低复杂度动作（P1）

1. 实现庄园快速收取（106），明确普通/快速请求字段和快速收取的资源消耗规则。
2. 实现军团税收状态查询与领取（116）。
3. 实现阴魂大厅进入与成功回包确认（117）。

验收：每项均有请求/响应编解码、自检夹具、服务端状态前后对比和幂等判断；默认不在未配置预算时消耗稀缺道具。

### 阶段 D：资源消耗类动作（P2）

依次实现商店购买（102）、普通锻造（103）、联盟捐献（111）、人物送礼（114）、精炼（115）和高温锻造（120）。

统一要求：

- 所有选品动作使用显式白名单，不自动选择高价值或锁定物品。
- 所有消耗动作支持单项与单日预算上限，先查询库存和服务端剩余次数。
- 只执行完成任务所需的最小次数；服务端进度未增加时立即停止。
- 每项分别保存业务结果，不记录令牌、游戏 token、会话密码或完整加密载荷。

验收：每项在成功、资源不足、功能未解锁、当日上限和重复执行场景下均有测试；实际执行后 `finished=true` 且领取积分后 `getscore=true`。

### 阶段 E：战斗与异步玩法（P3）

依次实现地下城（107）、公会委托（108）、骑士比武（109）、聚宝之地（110）、裂境角逐（113）和无序迷境（118）。

实施要点：

- 战斗类复用龙痕竞技场已经验证的战斗登录、遗留战斗恢复、开始握手、超时和结算状态机，但每个玩法使用自己的协议和胜负条件。
- 公会委托需要区分“派遣成功”与“委托结算”，本任务只以服务端派遣次数增加为准。
- 龙痕竞技场要求胜利；其他写明“完成挑战”的任务按服务端条件计数，不自行把进入或失败等同于完成。
- 聚宝之地必须确认“大宝箱”类型，普通宝箱不计数。

验收：每项都有完整成功链路、失败/超时恢复、登录期遗留战斗处理、任务进度确认和重复执行保护。

### 阶段 F：统一每日入口

新增统一命令，例如：

```bash
../.venv/bin/python daily_tasks.py status
../.venv/bin/python daily_tasks.py run --only 104,105,112
../.venv/bin/python daily_tasks.py claim
```

`run` 的固定流程：查询状态 -> 过滤未解锁/已完成任务 -> 计算剩余次数 -> 执行动作 -> 再查状态 -> 领取任务积分 -> 领取活跃奖励 -> 输出汇总。

验收：单次运行结果能分别列出已完成、跳过、未解锁、资源不足、失败和待重试任务；中断后可继续；同一天重复运行不重复消费；跨日后能按服务端刷新时间重新执行。

### 阶段 G：业务接入与界面交付

1. 将阶段 A/B 的日常状态、奖励闭环和动作适配器接入阶段 0 的任务表与单作业队列。
2. 接入真实账号登录、区服列表与连接状态；模拟账号服务测试通过后只做真实只读验证。
3. 将龙痕竞技场胜利选择重构为通用模式，确认处决 ID 后接入轮数与分段控件。
4. 联调任务勾选持久化、逐项进度、协作式停止、错误恢复和脱敏事件日志。
5. 完成 API、端到端和 Playwright 视觉测试，并提供固定版本依赖、启动脚本和使用说明。

验收：从空配置启动后，用户可只通过界面完成登录、选择区服、保存日常勾选、执行已选日常，以及按指定轮数和仁慈/处决模式运行龙痕竞技场；全流程不需要编辑 JSON 或输入 CLI 参数。

## 9. 全局验收标准

每个任务只有同时满足以下条件，才从“未完成”改为“已完成”：

1. 有独立可调用的业务动作，不依赖手工点击游戏界面。
2. 动作前读取服务端进度，只补足目标剩余次数。
3. 校验业务响应成功，并再次读取任务状态确认 `finished=true`。
4. 调用 `19702` 后确认 `getscore=true`，达到奖励阈值时调用 `19704` 并确认已领取列表更新。
5. 本地协议、模拟完整会话、错误分支和幂等测试全部通过。
6. 至少保留一份脱敏的真实成功结果或可复核的服务端状态记录。
7. 日志不得包含账号令牌、临时游戏 token、会话密码和完整原始载荷。

## 10. 本次验证记录

- `../.venv/bin/python harvest_fief.py --self-test`：通过。
- `../.venv/bin/python harvest_furnace.py --self-test`：通过。
- `../.venv/bin/python claim_monthly_vip_daily_rewards.py --self-test`：通过。
- `../.venv/bin/python explore_arcane_tower_daily_free.py --self-test`：通过。
- `../.venv/bin/python dragon_arena.py --self-test`：通过。
- `../.venv/bin/python adventurer_guild_daily_auto_refresh.py --self-test`：通过。
- `../.venv/bin/python engrave_ancient_law_court_daily_free.py --self-test`：通过。
- 当前全部 10 个 Python 文件执行 `../.venv/bin/python -m py_compile *.py`：通过。
- [`庄园收获进度.md`](庄园收获进度.md) 记录了 1 次普通庄园收取的真实成功结果。
- [`dragon_arena_websocket_analysis.md`](dragon_arena_websocket_analysis.md) 记录了龙痕竞技场真实胜利与结算链路。
- [`arcane_tower_daily_free.jsonl`](arcane_tower_daily_free.jsonl) 的当前记录显示池 1 当日已探索 1 次且免费次数为 0；该次检查未再次发起探索请求，因此不把它表述为脚本的真实成功调用证据。
- [`ancient_law_court_daily_free.jsonl`](ancient_law_court_daily_free.jsonl) 记录了古律院免费铭刻 `result=0`、免费次数 `1 -> 0` 且未检测到成本。
- 本次仅执行本地配置校验、协议自检和编译检查，没有发起新的线上业务操作。
