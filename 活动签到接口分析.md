# 活动签到接口与传输分析

## 范围与结论

本文基于已解出的 Unity JavaScript 资源进行静态分析，未向任何服务器发起请求，也未执行签到操作。

活动签到不是 HTTP/REST 接口：客户端将业务消息发到已登录游戏服的原生 WebSocket 连接中。实际领取命令为 `Activity_do_signin`（消息号 `21000`），补签命令为 `Activity_signin_remedy`（消息号 `21001`）。两者的请求体均只有活动 ID；账号身份、会话状态和资格校验不在这条请求体中，而依赖前置登录后的游戏会话。

静态资源中没有固定的签到 URL、HTTP 路径或域名。游戏服地址来自当前选区的 `selected.url`，再由 `TJWebSocket` 建连；因此只能确认协议为 WebSocket，不能仅凭本地代码确认实际使用 `ws://` 还是 `wss://`，也不能确认当前线上服务器地址。

## 活动中的签到类型

`ActivityModule` 从 `activity_signin` 表加载签到定义，并以 `uitype` 将运行时活动数据归入三个基础类型：`Normal=1`、`Soul=2`、`Return=3`。另外，`pay_signin` 表中的记录会指向首充签到所使用的 `activity_signin` ID。

| 类型 | 静态识别方式与条件 | 展示/行为 | 领取命令 |
| --- | --- | --- | --- |
| 普通签到 | `activity_signin.uitype=1`（`Normal`） | 7 项奖励时展示 `SigninPanel1New`；可由点击或自动展示逻辑领取。 | `Activity_do_signin` / `21000` |
| 灵魂签到 | `activity_signin.uitype=2`（`Soul`） | 7 项奖励时展示 `SigninPanel1`；点击当天奖励领取。 | `Activity_do_signin` / `21000` |
| 回归签到 | `activity_signin.uitype=3`（`Return`） | 7 项奖励时展示 `SigninReturnPanel`；除基础奖励外，还可在满足 `extra_ticket` 时领取额外奖励。两者使用同一业务命令。 | `Activity_do_signin` / `21000` |
| 首充签到 | `pay_signin` 的 ID `1` 记录提供 `signinid`，缺省回退为 `301`；要求 `FirstBuy` 功能解锁且累计付费金额大于 0。 | 本质仍是一个 `activity_signin` 活动，且未发现独立的首充签到业务消息。已解出模块未包含支付入口到具体面板的最终路由，因此“首充按钮直接调用 `21000`”属于由通用活动模型支持的高置信推断，而非已定位的点击调用点。 | 无独立命令；通用领取命令为 `Activity_do_signin` / `21000` |
| 15 天/30 天展示变体 | 不属于新的 `uitype`。`rewards` 按 `#` 切分后，15 项使用 `SigninPanel2`，30 项使用 `SigninPanel3`。 | 两个面板当前代码中的 `canRemedy()` 固定返回 `false`，且没有手工领取发送逻辑；`ActivityModule.checkShowDailySignin()` 实现了展示后自动发送基础领取的通用路径，但其外部触发点未包含在当前解出模块中。 | 通用领取命令 `Activity_do_signin` / `21000` |
| 补签 | 旧版 7 天 `SigninPanel` 的遗漏日期处理；仅允许未签到、未锁定且此前无待补日期的最早日期。费用来自 `activity_signin.remedy_costs`。 | 用户确认消耗后发起补签。15 天/30 天面板未启用补签。 | `Activity_signin_remedy` / `21001` |

所有实际可见活动均由服务端下发的 `signinData.acts` 决定。静态代码可以确认上述类型和规则，但不能在没有登录态与运行时配置的情况下列出当前服务器实际开放的活动 ID 或活动数量。

以下内容虽然名称中带“签到”，但不属于本次“活动签到”范围：公会签到使用独立命令 `Guild_daily_signin`（`20030`），不经过 `Activity_do_signin`。

## 业务接口

### 1. 领取签到：`Activity_do_signin`

| 项目 | 已确认内容 |
| --- | --- |
| 消息号 | `21000` |
| C2S 编码器 | 导出名 `xk1`，内部对象 `oy` |
| C2S Protobuf 字段 | `id`：字段号 `1`，类型 `int32`，值为目标 `activity_signin` 活动 ID |
| S2C 解码器 | 导出名 `N6x`，内部对象 `sy` |
| S2C 字段 | `id`（#1，`int32`）、`act`（#2，活动状态）、`ret`（#4，`int32`）、`msg`（#5，`string`） |
| 成功处理 | `ret == 0` 时，客户端用 `act` 更新 `elapsedDays`、`signinData`、`limitedTimeSigninInfo` 与 `ticket`，再触发 `activity_signin_suc`。 |

各签到面板构造的业务对象等价于：

```text
{ id: <当前活动 ID> }
```

没有发现该请求额外携带日期、奖励下标、账号、支付金额、时间戳、签名或令牌字段。客户端仅在本地先检查 `todaySigned`、签到位图、资格票券等条件；服务端是否以及如何重复校验，无法由客户端静态代码确认。

调用链：

```text
签到面板点击 / 自动展示逻辑
  -> SocketManager.instance.sendMsg(Activity_do_signin, { id }, xk1)
  -> xk1.encode({ id }).finish()
  -> MsgHdr.encode({ id: 21000, data: <签到 Protobuf> })
  -> Pack1Encode(..., 会话密码, 100)
  -> NativeWebSocketImpl.send(string)
  -> TJWebSocket.SendText(...)
```

### 2. 补签：`Activity_signin_remedy`

| 项目 | 已确认内容 |
| --- | --- |
| 消息号 | `21001` |
| C2S 编码器 | 导出名 `ACD`，内部对象 `ry` |
| C2S Protobuf 字段 | `id`：字段号 `1`，类型 `int32`，值为目标活动 ID |
| S2C 解码器 | 导出名 `YLE`，内部对象 `ly` |
| S2C 字段 | `id`（#1）、`act`（#2）、`ret`（#4）、`msg`（#5）；字段含义与普通签到响应一致。 |
| 成功处理 | `ret == 0` 时，`ActivityModule.updateData(act)` 更新活动状态，并触发 `Activity_signin_remedy_suc`。 |

补签请求同样不携带消耗道具或补签日期。客户端以活动 ID 请求，服务器端根据活动状态和规则决定实际补哪一天、是否接受以及扣除何种费用。

### 3. 状态同步：`Activity_signin_sync`

该消息不是领取动作，但与签到状态维护直接相关：

| 项目 | 已确认内容 |
| --- | --- |
| 消息号 | `21002` |
| 请求 | `b0n` / `cy`，仅 `id`（字段 #1，`int32`） |
| 响应 | `Ft6` / `dy`，包含 `id` 与 `act` |
| 用途 | 活动状态中的 `dailyResetRemainSecs` 到期后，客户端定时发送同步请求。 |

## WebSocket 帧与加密方式

### 外层 Protobuf 信封

`SocketManager.send()` 将业务 Protobuf 放入 `MsgHdr`。已确认的字段如下：

| 字段 | 字段号 | 类型 | C2S 签到时的用途 |
| --- | --- | --- | --- |
| `id` | 1 | `int32` | 业务消息号，例如 `21000` 或 `21001` |
| `sid` | 2 | `int32` | 服务端序号字段；签到请求中客户端不显式设置 |
| `data` | 4 | `bytes` | 内层签到/补签 Protobuf 字节串 |
| `contextId` | 6 | `int32` | 未在签到调用中设置 |
| `msgName` | 7 | `string` | 未在签到调用中设置 |
| `extraHeader` | 50 | 嵌套消息 | 未在签到调用中设置 |

因此，`21000`/`21001` 是 WebSocket 消息信封中的业务消息号，不是 HTTP 路径。

### `Pack1` 编码流程

签到调用未显式关闭 `crypt` 参数；`SocketManager.send()` 的默认值为 `true`，且 `isCryptMsg()` 在该客户端构建中恒为真。发送时使用 `CryptUtil.Pack1Encode`，应用启动入口将其运行时实现设为 `NodeRuntime -> NodeCrypto`。

对编码后的 `MsgHdr` 字节串，`Pack1Encode` 的顺序为：

```text
MsgHdr Protobuf 字节
  -> 若长度 > 100 字节：zlib deflate（BEST_COMPRESSION）
  -> 写入 4 字节小端原始长度前缀
       - 未压缩时前缀为 0
       - 压缩时前缀为压缩前长度
  -> 若会话密码长度 >= 8：DES-ECB，密钥为会话密码前 8 个字符，IV 为 null
  -> Base64 文本
  -> WebSocket 文本帧（TJWebSocket.SendText）
```

反向接收路径为 `Pack1Decode -> MsgHdr.decode -> 按消息号分发 -> 对应活动响应解码器`。`Pack1Decode` 先做 Base64 解码，满足密码条件时 DES-ECB 解密，再按 4 字节前缀决定是否 zlib 解压。

结论：这是“Protobuf + 可选压缩 + DES-ECB + Base64”的应用层封装，而不是 JSON 请求体、HTTP 参数加密或 AES。Base64 仅是编码，不提供保密性；实际 DES 分支是否执行取决于会话密码长度，正常已登录签到流程会等待会话密码就绪后再发送。

### 会话密码建立

1. WebSocket 打开后，客户端先清空 `msgPassword`，发送登录消息，且该登录调用显式传入 `crypt=false`。
2. 客户端收到 `Pack_password` 消息后，调用 `Pack1Decode(t.p, getMK())` 得到会话密码，并通过 `SocketManager.setMsgPwd()` 保存。
3. 收到 `Login_reunique` 后才设置 `msgReady=true`，随后发送此前缓存的业务消息，包括签到请求。

本文不记录会话密码、引导密钥材料或用于重放/伪造消息的步骤。静态代码只足以确认其生成与使用链路，不能证明服务端侧的会话校验、重放防护或风控策略。

## 连接方式与接口定位

| 层级 | 实现 | 结论 |
| --- | --- | --- |
| 游戏服地址 | `ServerManager.getGameUrl()` 返回 `selected.url` | 地址由选区/运行时服务器列表提供，不是签到模块常量。 |
| Socket 实现 | `NativeWebSocketImpl` 创建 `new TJ.TJWebSocket(url)` | 使用 Unity 原生 WebSocket 桥接。 |
| 发送格式 | 字符串走 `TJWebSocket.SendText`，二进制才走 `Send` | 本路径的 `Pack1Encode` 返回 Base64 字符串，因此签到为 WebSocket 文本帧。 |
| HTTP 路径 | 未发现 | 不存在可从本地静态代码确认的 `POST /...` 签到路径。 |

若需要获取某个授权账号所连的实际主机、端口和 `ws`/`wss` 方案，应仅在受授权的本地测试环境中观察建连元数据；不应对线上账号实际提交签到消息。

## 代码证据

已解密文件位于临时目录 `/private/tmp/dungeon4-decrypted`；以下位置可用 `rg` 按符号名定位：

| 文件/模块 | 关键证据 |
| --- | --- |
| `main.js`，模块 `51072`（编译行约 1346-1354） | `ActivityModule`：`Normal/Soul/Return` 枚举、加载 `activity_signin` 与 `pay_signin`、类型选择、自动签到、响应状态更新。 |
| `main.js`，模块 `77095` | `Activity_do_signin=21000`、`Activity_signin_remedy=21001`、`Activity_signin_sync=21002` 的消息号定义，以及 `MsgHdr`。 |
| `main.js`，模块 `26136` | `xk1 -> oy`、`N6x -> sy`、`ACD -> ry`、`YLE -> ly`、`b0n -> cy`、`Ft6 -> dy` 的 Protobuf 编码/解码定义。 |
| `signinPanel1.js`、`signinPanel1New.js`、`itemSigninBox.js` | 点击处理器构造 `{id: signinId}` 并发送 `Activity_do_signin`。 |
| `signinReturnPanel.js` | 回归基础奖励和额外奖励均发送 `Activity_do_signin`。 |
| `signinPanel.js` | 补签确认后构造 `{id: activityId}` 并发送 `Activity_signin_remedy`。 |
| `signinPanel2.js`、`signinPanel3.js` | 15/30 天展示变体；`canRemedy()` 固定返回 `false`，没有手工发送签到/补签调用。 |
| `main.js`，模块 `23307` | `SocketManager.sendMsg`、Protobuf 封装、加密发送、接收解密与分发。 |
| `main.js`，模块 `85299` | `NativeWebSocketImpl`，字符串调用 `TJWebSocket.SendText`。 |
| `main.js`，模块 `33349` 与 `95568` | `NodeCrypto.Pack1Encode/Pack1Decode` 和 `NodeRuntime`：zlib、DES-ECB、Base64 实现。 |
| `main.js`，应用启动入口 | `App.setRuntime(new NodeRuntime())`，确认运行时使用上述 `NodeCrypto` 实现。 |

## 静态分析边界

- 未确认当前生产环境实际活动 ID、活动配置、游戏服主机、端口、WebSocket URL 或 TLS 方案。
- 未在当前解出的模块中定位首充入口和 `checkShowDailySignin()` 的外部调用点；首充与 15/30 天变体到通用领取命令的关联已由同一活动数据模型和通用代码路径支持，但仍保留上述边界。
- 未确认服务端对活动资格、补签费用、重复领取、并发、重放和账号会话的校验逻辑。
- 未发现签到请求中的额外签名、HMAC、时间戳或 nonce 字段；这只说明客户端该消息体和信封没有这些字段，不能据此推断服务端不存在其他会话级安全控制。
- 未发送网络请求，文中所有结论均来自本地客户端代码。
