# 萃华仪收获接口分析

## 结论

萃华仪的“收获”按钮发送的是已登录游戏服上的加密 WebSocket 业务消息，不是独立 HTTP 接口。

| 项目 | 值 |
| --- | --- |
| 业务消息 | `Furnace_capture` |
| 消息号 | `19298` |
| 普通收获参数 | `{ mode: 0, type: 0 }` |
| C2S 字段 | `mode` (#1, int32)、`type` (#2, int32)、`times` (#3, int32) |
| S2C 字段 | `ret` (#1)、`mode` (#2)、`furnace` (#3)、`soul` (#4)、`soulRet` (#5) |
| 材料通知 | `Storage_notify_itemchange` (`12602`)，普通收获 `source = FURNACE_CAPTURE_NORMAL = 460` |

普通按钮调用在 `decrypted-js/main.js:8640`：

```javascript
{ mode: 0, type: 0 }
SocketManager.instance.sendMsg(Furnace_capture, payload, FurnaceCaptureCodec)
```

三个字段都是 Protobuf 默认值，因此编码器不会写入内层 C2S 字段。最终业务包的内层 `MsgHdr` 只包含 `id = 19298`，不包含 `data` 字段。

## 会话与封包

1. 从本地 `tokens.json` 读取 `userid` 与 `verify_token`。
2. 账号服 `Logincheck` 返回临时游戏 token、区服列表和网关地址。
3. 区服网关使用 `{ zid, token }` 返回游戏 WebSocket 地址。
4. 首包发送未加密 `Login` (`10010`)；收到 `Pack_password` (`10090`) 后取得会话密码；收到 `Login_reunique` (`10022`) 后等待客户端同样使用的 100 ms 再发送业务消息。
5. `Furnace_capture` 使用 Pack1：原始 `MsgHdr` 前置 4 字节小端长度、DES-ECB/PKCS#7、Base64，作为 WebSocket 文本帧发送。
6. 收到 `Furnace_capture` 且 `ret = 0` 即为收获成功；随后读取 `Storage_notify_itemchange` 中 `source = 460` 的材料变动。

`decrypted-js/main.js:7799` 定义 `Furnace_capture = 19298`；`decrypted-js/main.js:8615` 注册其响应处理；`decrypted-js/main.js:8631` 在 `ret == 0` 时更新炉子与灵魂数据。

## 可调用脚本

```bash
../.venv/bin/python harvest_furnace.py
```

指定区服：

```bash
../.venv/bin/python harvest_furnace.py --zone-id 4101
```

只校验本地协议和模拟会话，不访问网络：

```bash
../.venv/bin/python harvest_furnace.py --self-test
```

脚本不会打印本地令牌、临时游戏 token 或会话密码。真实验证已收到 `Furnace_capture.ret = 0`，并收到 `source = 460` 的材料通知。
