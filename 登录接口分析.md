# 登录接口分析

## 范围与结论

本目录是已解包的 Unity Android 应用，不含服务端源码。以下结论来自对 DEX 的静态反编译和资源配置检查；未向任何线上接口发起请求。

应用包名为 `com.zygames.dungeon4`，登录由筑月渠道 SDK（`com.zysdk.chal_zhuyue`）实现。账号密码、短信、一键手机号及 TapTap 登录均先由筑月认证服务签发身份令牌，再由渠道验签接口换取游戏侧令牌。

```text
账号密码 / 短信 / 一键手机号 / TapTap
  -> https://ocplatform.gamelunar.com/passport/*
  -> id_token + refresh_token
  -> /passport/channel_signin
  -> verify_token + userid + pay_token
  -> Unity 游戏收到 userid + verify_token
```

渠道配置：

- `ZY_CHANNEL_ID=110001`
- `ZY_MEDIA_ID=M521957`
- `media=M521957`

## 核心服务

认证基础地址：`https://ocplatform.gamelunar.com`

接口常量位于反编译代码的 `com.zysdk.chal_zhuyue.constants.ZYHttpConstants`。

| 接口 | 用途 | 静态确认的请求字段 |
| --- | --- | --- |
| `POST /passport/signin` | 账号密码或短信登录 | `utype`、`username`、`password` 或 `smscode`、`gamecode`、`ip`、`devinfo` |
| `POST /passport/smscode` | 下发短信验证码 | `phonenumber`、`gamecode` |
| `POST /passport/signin_with_onetap` | 运营商一键登录 | `gamecode`、`token`、`carrier`、`appid`、`ip`、`devinfo` |
| `POST /passport/signin_with_bind` | TapTap 已绑定账户登录 | `game`、`bid`、`token`、`ntype=4` |
| `POST /passport/bind_account` | 将 TapTap 账户绑定到手机号账户 | `idtoken`、`bid`、`token`、`ntype=4` |
| `POST /passport/refresh_token` | 静默刷新身份令牌 | `token`，值为本地保存的 `refresh_token` |
| `POST /passport/channel_signin` | 将渠道身份令牌换成游戏令牌 | `openid`、`idtoken`、`game`、`channel`、`ip`、`devinfo`、`termininfo`、`media` |
| `POST /passport/passwd_recovery` | 验证或重置密码 | `username`、`smscode`、`game`、`passwd` |
| `POST /passport/bindidcard` | 实名认证 | `token`、`gamecode`、`idname`、`idcode` |

`utype` 的值为 `0` 时对应手机号路径，值为 `1` 时对应非手机号账号路径。客户端对手机号仅进行 `^1\\d{10}$` 格式检查；服务端的账号、密码、验证码和令牌校验无法由本地 APK 确认。

## 登录过程

### 1. 账号密码

`ZYPasWordLoginAct.loginAction()` 构造下列参数并调用 `/passport/signin`：

```text
utype=<0: 手机号, 1: 普通账号>
username=<输入账号>
password=<输入密码>
gamecode=<游戏标识>
ip=<本地 IP>
devinfo=<Android ID>
```

### 2. 短信登录

先调用 `/passport/smscode` 申请验证码：

```text
phonenumber=<手机号>
gamecode=<游戏标识>
```

输入验证码后，仍调用 `/passport/signin`：

```text
utype=0
username=<手机号>
smscode=<短信验证码>
gamecode=<游戏标识>
ip=<本地 IP>
devinfo=<Android ID>
```

### 3. 一键手机号登录

运营商 SDK 返回授权令牌后，客户端调用 `/passport/signin_with_onetap`。`appid` 来自 Android Manifest 的 `onetouch_appid` 元数据。

### 4. TapTap 登录

TapTap 授权成功后，客户端将 TapTap `mac_key` 作为 `bid`、`access_token` 作为 `token`，并以 `ntype=4` 调用 `/passport/signin_with_bind`。若服务端要求绑定手机号，则短信登录成功后调用 `/passport/bind_account` 完成绑定。

### 5. 令牌刷新和游戏验签

认证成功的典型响应由客户端按以下结构读取：

```json
{
  "code": 0,
  "data": {
    "id_token": "...",
    "refresh_token": "..."
  }
}
```

客户端将 `id_token` 视作 JWT 形态字符串，Base64 解码中间段读取实名、年龄、`openid` 等信息。之后它调用 `/passport/channel_signin`，使用 `openid` 和 `idtoken` 换取：

```json
{
  "code": 0,
  "data": {
    "verify_token": "...",
    "userid": "...",
    "pay_token": "...",
    "New": 0
  }
}
```

最终交给 Unity 层的登录结果是 `userid + verify_token`，而不是原始 `id_token`。本地 `refresh_token` 存在时，SDK 会先调用 `/passport/refresh_token` 做静默登录。

## 重要实现细节

### 请求实际写入 URL query

请求辅助类名为 `jsonPost`，但它没有序列化 JSON 请求体。实现先将参数拼接至 URL，再建立 `POST` 连接，并仅打开和关闭空输出流。因此实际形态类似：

```text
POST /passport/signin?username=...&password=...&gamecode=... HTTP/1.1
Content-Type: application/x-www-form-urlencoded

<empty body>
```

参数值会经过 URL 编码，但敏感数据仍位于 URL 中。

### 本地令牌保存

SDK 将以下值保存到名称为 `config` 的私有 `SharedPreferences`：

- `id_token`
- `refrsh_token`（键名拼写如此）
- `verify_token`
- `pay_token`
- 用户标识及游戏标识

登出逻辑仅清空本地偏好项；在本 APK 的渠道 SDK 代码中未发现对应的服务端 token 撤销请求。

## 风险发现

### 严重：HTTPS 证书和域名校验完全失效

`ZYHttpKit` 为所有 `HttpsURLConnection` 设置自定义 `SSLSocketFactory` 和 `HostnameVerifier`：

- `TrustAnyTrustManager.checkServerTrusted()` 为空；
- `TrustAnyHostnameVerifier.verify()` 永远返回 `true`。

影响：攻击者在可控网络路径上可使用任意证书进行中间人攻击，读取或篡改账号密码、短信验证码、运营商 token、`id_token`、`refresh_token`、`verify_token` 与实名信息。

### 严重：凭据和令牌通过 URL 发送

由于请求参数拼入 query string，密码、短信验证码和各类令牌可能进入代理、负载均衡、WAF、Web 服务器访问日志、崩溃日志或诊断系统。即使采用 HTTPS，此泄露面依然存在。

### 高：生产日志输出敏感信息

`ZYLogUtils.isEnable` 默认值为 `true`，且未发现关闭它的代码。SDK 会记录：

- 完整请求 URL 与参数；
- 完整响应；
- `id_token` 中间段解码出的用户信息；
- `verify_token`、`pay_token`、用户 ID；
- TapTap 授权 token。

影响：具有设备调试能力、日志收集权限或可读取诊断日志的主体可能获得可复用凭据。

### 中：长期令牌以明文保存

`id_token`、`refresh_token`、`verify_token`、`pay_token` 使用普通 `SharedPreferences` 保存，未使用 Android Keystore 或加密存储。Root 设备、恶意备份、调试环境或受损设备上的数据提取会增加会话劫持风险。

### 中：静态全局回调可能导致请求响应错配

`ZYHttpKit.mCallback` 是静态全局变量。每次请求均会覆盖它，而线程池允许并发请求。多个认证、取 IP、短信、支付等请求同时进行时，响应可能被分发至最后注册的回调，造成错误状态转换或错误处理。

### 中：客户端只对 JWT payload 做 Base64 解码

客户端从 `id_token` 的中间段读取实名和年龄状态，但本地未验证 JWT 签名。客户端显示逻辑不能作为授权依据；服务端必须独立校验签名、签发方、受众、过期时间和用途。

## 修复优先级

1. 删除宽松 `TrustManager` 与 `HostnameVerifier`，恢复系统 TLS 校验；对认证域名实施证书固定时需提供安全的证书轮换方案。
2. 将所有敏感参数改为 `POST` 请求体，推荐 JSON 或表单 body；严禁将密码、验证码和 token 放入 URL。
3. 生产环境关闭敏感日志，日志中仅保留脱敏账号、请求 ID、状态码和必要错误信息。
4. 用 Android Keystore 支持的加密存储保存 refresh/access token；缩短令牌有效期、做轮换，并补充服务端撤销接口。
5. 为每个网络请求绑定独立回调或使用成熟 HTTP 客户端的请求对象，移除全局静态回调。
6. 服务端对短信发送、登录尝试、密码重置和一键注册实施限流、设备/IP 风控及审计；错误信息避免泄露账户存在性。
7. 游戏服务在每次需要鉴权时验证 `verify_token`，不可信任客户端解析的身份字段或本地状态。

## 关键证据位置

反编译输出临时位于 `/private/tmp/dungeon4_jadx/sources`：

- `com/zysdk/chal_zhuyue/constants/ZYHttpConstants.java`：认证接口地址。
- `com/zysdk/chal_zhuyue/utils/ZYHttpKit.java`：TLS 绕过、请求拼接、请求/响应日志、全局回调。
- `com/zysdk/chal_zhuyue/login/ZYPasWordLoginAct.java`：账号密码登录参数。
- `com/zysdk/chal_zhuyue/login/ZYInputCodeAct.java`：短信登录和 TapTap 绑定参数。
- `com/zysdk/chal_zhuyue/login/ZYCodeLoginAct.java`：短信发送、一键登录、TapTap 登录参数。
- `com/zysdk/chal_zhuyue/login/ZYtimeCountAct.java`：静默刷新令牌。
- `com/zysdk/chal_zhuyue/utils/ZYSaveInfoUtils.java`：认证响应解析、本地令牌保存、JWT payload 解码。
- `com/zysdk/chal_zhuyue/utils/ZYSPUtils.java`：明文 SharedPreferences 存储。
- `com/zysdk/sdk/ZhuyueSDK.java`：`channel_signin` 二次验签和最终 Unity 回调。
- `assets/zhuyue.properties`、`assets/leiting.properties`：渠道与媒体标识。
