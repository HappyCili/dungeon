# 登录说明

`login.py` 实现渠道 SDK 的两段式认证：先获取身份令牌，再交换游戏侧使用的 `verify_token`。

```text
身份登录
  -> id_token + refresh_token
  -> channel_signin
  -> userid + verify_token + pay_token
```

## 环境

在项目根目录执行命令，并使用项目虚拟环境：

```bash
cd /Users/max/Downloads/dungeon4_M521957
```

脚本已内置当前安装包和模拟器对应的默认 `game`、`ip`、`device-info` 与 `terminal-info` 参数。网络出口变化时可通过 `--ip` 覆盖默认值。

## 账号密码登录

```bash
../.venv/bin/python login.py password --username ACCOUNT
```

运行后会显示 `密码：`。输入过程不回显字符，输入完成后按回车即可。

成功时终端仅显示脱敏后的令牌摘要，并把完整令牌保存至项目根目录的 `tokens.json`。

## 短信验证码登录

该命令使用已获取的短信验证码登录：

```bash
../.venv/bin/python login.py sms \
  --phone PHONE_NUMBER \
  --sms-code SMS_CODE
```

## 刷新登录态

已有 `tokens.json` 时，可使用其中的 `refrsh_token` 刷新身份令牌并再次交换游戏令牌：

```bash
../.venv/bin/python login.py refresh
```

`refrsh_token` 的拼写与 Android SDK 的本地存储键保持一致。

## 常用参数

| 参数 | 作用 |
| --- | --- |
| `--game` | 覆盖 SDK 的 `gamecode`。 |
| `--ip` | 覆盖请求中的客户端 IP；网络出口变化时需要更新。 |
| `--device-info` | 覆盖请求中的 Android `devinfo`。 |
| `--terminal-info` | 覆盖 `channel_signin` 的终端信息。 |
| `--channel` | 覆盖渠道 ID。 |
| `--media` | 覆盖媒体 ID。 |
| `--base-url` | 覆盖认证服务基础地址。 |
| `--timeout` | 设置 HTTP 超时时间，单位为秒。 |
| `--ca-file` | 指定 CA 证书文件。 |
| `--token-file` | 覆盖令牌文件路径。 |

例如，网络出口变化后可临时覆盖 IP：

```bash
../.venv/bin/python login.py password \
  --username ACCOUNT \
  --ip NEW_IP
```

## 令牌文件

默认令牌文件为：

```text
./tokens.json
```

文件包含 `id_token`、`refrsh_token`、`verify_token`、`pay_token`、`userid` 与 `openid`，脚本写入后会设置为 `0600` 权限。不要把该文件提交到版本控制或发送给他人。

如需使用其他位置：

```bash
../.venv/bin/python login.py password \
  --username ACCOUNT \
  --token-file /PATH/TO/tokens.json
```

## TLS 证书

脚本保持 TLS 证书和主机名校验开启。当前 Python 环境会自动使用系统证书包 `/etc/ssl/cert.pem`。

若使用其他 Python 环境仍出现证书链错误，可显式指定可信 CA 文件：

```bash
../.venv/bin/python login.py password \
  --username ACCOUNT \
  --ca-file /PATH/TO/ca-bundle.pem
```

## 常见问题

| 提示 | 处理方式 |
| --- | --- |
| `令牌文件不存在` | 先完成一次账号密码或短信登录，再运行 `refresh`。 |
| `SSL: CERTIFICATE_VERIFY_FAILED` | 检查 `/etc/ssl/cert.pem` 是否存在，或通过 `--ca-file` 指定可信 CA 文件。 |
| `网络错误` | 检查网络、DNS 与认证服务连通性；网络出口变化时使用 `--ip` 更新请求参数。 |
| 服务端拒绝请求 | 检查账号信息、验证码、默认参数及当前网络状态。 |
