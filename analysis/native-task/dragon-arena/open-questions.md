# 待确认项

- `Scararena_challenge.ret=2` 的精确服务端业务语义未在本地字符串表中找到，当前仅将其原样报告。
- 已确认它是候选级拒绝：请求与响应的序号一致、没有对手数据或战斗帧，原生会刷新 `21100`。
- `mdata.challenge=false` 表示该位置尚未胜利通过；它不承诺本次 `21104` 可成功，因此无法从该字段区分 `ret=2` 的具体服务端前置条件。
