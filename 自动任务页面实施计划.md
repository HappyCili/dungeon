# 自动任务页面实施计划

## 目标

在现有单页操作台增加“自动任务”标签，提供共享任务选择的每日执行与固定间隔定时执行。每次运行只处理当前满足服务端条件的项目，并复用现有串行作业、会话恢复、停止与日志机制。

## 执行项目与顺序

1. 遍历并领取全部可领取签到。
2. 收取庄园普通产出，不执行快速或付费收取。
3. 领取盈月之仪每日奖励。
4. 领取萃华仪可用奖励。
5. 领取龙痕竞技场每日奖励，并对服务端列出的未点赞对象点赞。
6. 按当前聚宝地图的剩余免费扫荡次数分批扫荡。
7. 按普通竞技场剩余免费挑战次数挑战。
8. 按“当日获得龙痕币”或“背包龙痕币总量”目标挑战龙痕竞技场。
9. 使用地下城页当前选中的地下城扫荡一次，并全部抽取本次可用宝库奖励。
10. 最后读取日常/周常状态，领取服务端已确认完成任务的积分和所有可领取活跃奖励。

## 页面与配置

- 自动任务标签显示每项的复选框、就绪状态、最近执行结果和跳过原因。
- 每日执行提供“刷新条件”“立即执行”“停止”命令。
- 定时执行使用开关和 5 到 1440 分钟的间隔输入，默认 60 分钟；显示上次检查、下次检查及跳过原因。
- 每日执行和定时执行共用一套勾选项。地下城扫荡复用地下城页的当前选择；配置保存任务选择、调度开关、间隔、龙痕目标模式和目标值；不保存令牌、密码或会话地址。

## 后端设计

- 自动任务服务负责条件快照、顺序编排、单项结果和聚合结果。
- 定时调度器只在本机应用进程存活期间运行；作业忙碌时跳过当轮，不补跑。
- 路由提供自动任务状态、配置保存和手动启动接口，自动启动的作业继续使用通用作业轮询与取消接口。
- 未领取、无免费次数、已达目标或活动未开启均作为正常跳过；会话故障结束本批作业。

## 验证

- 单测覆盖配置迁移、条件筛选、固定执行顺序、地下城奖励透传、日常/周常奖励领取、取消、调度跳过与龙痕目标模式。
- 路由测试覆盖自动任务配置校验、启动冲突和作业返回结构。
- 浏览器检查新增标签、禁用状态、运行进度和窄屏布局。

## 龙痕奖励与点赞校验（2026-07-30）

- 领取每日奖励后，无论奖励是否可领取，均继续查询龙痕排行榜并尝试点赞。
- 原生 `CLeaderboardGetList` 请求使用 `kind` 字段 #4，龙痕竞技场值为 `6`；首屏的 `page`、`grp`、`innergrp` 都为默认 `0`，编码时省略。
- 响应的排行榜条目位于字段 #4，条目的 UID 位于字段 #3；筛除本人和 `Scararena_info.likenum` 中已有的 UID 后，逐个发送 `CLeaderboardLike`（`kind` #1、`uid` #3）。
- 运行日志必须显示：查询前的剩余点赞次数、排行榜返回条数、符合条件的候选数、每次请求的 UID 与回包结果。这样可区分服务端没有候选、筛选后为空和发送点赞失败。
- 回归测试固定断言排行榜请求载荷为 `kind=6`，并覆盖每日奖励 `ret=2` 时仍查询排行榜、筛除本人/已点赞对象，以及每一个候选均发送点赞请求。
