# 庄园收获接口分析

## 结论

点击庄园的普通“征收”不是 Java HTTP 接口，而是已登录游戏服上的加密 WebSocket 业务消息：

| 项目 | 值 |
| --- | --- |
| 业务消息 | `Fief_harvest_res` |
| 消息号 | `19154` |
| 普通征收模式 | `HARVEST_NORMAL = 0` |
| C2S 字段 | `mode` (#1, int32)、`factorytype` (#2, int32)、`times` (#3, int32) |
| S2C 字段 | `ret` (#1)、`mode` (#2)、`fiefdata` (#3)、`factorytype` (#4)、`times` (#5) |
| 材料通知 | `Storage_notify_itemchange` (`12602`)，`source = FIEF_HARVEST_RES = 37` |

普通点击构造 `{ mode: HARVEST_NORMAL }`。由于 `0` 是 Protobuf 默认值，客户端编码器不会写入该字段，因此内层 C2S Protobuf 为空；外层 `MsgHdr` 只携带 `id = 19154`，不写 `data` 字段。

## WebSocket 封包

沿用 [活动签到接口分析.md](活动签到接口分析.md) 的会话和封包流程：

1. 账号服以 `userid + verify_token` 换取游戏 token、区服列表和网关地址。
2. 区服网关以 `zid + token` 返回游戏 WebSocket URL。
3. WebSocket 先发送未加密 `Login` (`10010`)；收到 `Pack_password` (`10090`) 后得到会话密码；收到 `Login_reunique` (`10022`) 后会话可发送业务消息。
4. `Fief_harvest_res` 使用 `Pack1Encode`：内层 `MsgHdr` 在 100 字节内不压缩，前置 4 字节小端原始长度 `0`，DES-ECB/PKCS#7 加密，最后 Base64 作为 WebSocket 文本帧发送。
5. 收到 `Fief_harvest_res` 且 `ret = 0` 表示服务端确认征收；随后从 `Storage_notify_itemchange` 中筛选 `source = 37`，输出材料 ID、变化量和当前总数。

`ret = 4` 对应本地中文资源中的“暂无可收取资源”。普通收取成功后不应在同一作业中立即重复提交；日常 105 未满两次时，等待新的资源产出后再次执行即可。

当前 Android 构建的 SDK 渠道标识为 `taojin_android_zhuyue-110001`；脚本已将其设为默认值。
账号服当前把 `zid` 返回为数值，转发给区服网关时必须保持数值类型；转成字符串时网关会返回空对象。原生 WebSocket 回调也可能把 Pack1 的 Base64 数据交付为二进制 `Uint8Array`，因此脚本同时接受文本和二进制帧进行 Base64 解码。

## 代码位置

- `decrypted-js/main.js:719`：庄园模块注册 `Fief_harvest_res` 的响应解码器。
- `decrypted-js/main.js:730`：`autoGetOutput()` 构造 `mode = HARVEST_NORMAL` 并发送 `Fief_harvest_res`。
- `decrypted-js/main.js:2563`：C2S 编码器字段定义。
- `decrypted-js/main.js:7799`：`Fief_harvest_res = 19154`。
- `decrypted-js/main.js:1905`：材料变更通知结构，`items` 的 `id/num/sum` 字段。
- `decrypted-js/main.js:1483`：`FIEF_HARVEST_RES = 37`。

## 脚本

`harvest_fief.py` 使用标准库实现账号服请求、WebSocket、DES-ECB、Pack1 和所需 Protobuf 字段：

```bash
../.venv/bin/python harvest_fief.py
```

多个区服时可显式指定：

```bash
../.venv/bin/python harvest_fief.py --zone-id ZONE_ID
```

不访问网络的协议自检：

```bash
../.venv/bin/python harvest_fief.py --self-test
```
