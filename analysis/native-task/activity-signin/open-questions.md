# 活动签到：待确认项

1. 原生的“未满档”判断依赖 `activity_signin.rewards` 配置表。当前工作区没有加载该表的稳定路径，因此自动任务不推测奖励天数，领取结果仍由服务器 `ret` 作为最终判定。
2. 原生在 `dailyResetRemainSecs` 到期时发送单活动 `21002`。长期保持同一 WebSocket 到跨日时，需要结合运行时定时器或可靠的服务端时间状态再决定是否补充该同步。
3. 回归签到的额外奖励及补签分别有额外票券/费用条件；自动签到目前只覆盖基础的 `21000` 领取，不执行 `21001` 补签。
