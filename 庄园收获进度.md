# 庄园收获进度

更新日期：2026-07-21

## 当前状态

庄园普通征收与快速收取链路已定位并实现。`harvest_fief.py` 可从本地 `tokens.json` 读取登录凭据，自动完成账号服、区服网关、游戏服 WebSocket 登录，并按模式发送一次庄园材料征收。普通征收已完成真实账号验证；快速收取（`mode=free`）已通过本地协议自检与日常动作夹具，默认不消耗宝石。

## 已完成流程

1. 静态分析 Unity JavaScript 业务层，确认普通“征收”不是独立 Java HTTP 接口。
2. 定位庄园征收业务消息为 `Fief_harvest_res`，消息号 `19154`。
3. 确认普通征收使用 `HARVEST_NORMAL = 0`；该默认值不会被 Protobuf 编码，因此 C2S 内层请求为空，外层 `MsgHdr` 仅带 `id=19154`。
4. 确认快速收取复用同一消息：`HARVEST_FREE = 1`（免费，消耗 `leftfreenum`）、`HARVEST_PAY = 2`（付费，消耗宝石 ID `2`）。
5. 复用活动签到的 Pack1 会话协议：账号服换取游戏 token、区服网关获取游戏服地址、未加密 `Login`、`Pack_password` 协商会话密码、`Login_reunique` 后发送业务消息。
6. 实现标准库 Python 客户端，包括 HTTP 请求、RFC 6455 WebSocket、DES-ECB/PKCS#7、Pack1 和所需 Protobuf 编解码。
7. 解析 `Storage_notify_itemchange` (`12602`) 中 `source=37` 的材料通知，输出材料 ID、变化量和当前总数。
8. 日常任务 106 接入 `daily_actions.run_fief_quick_harvest_action`，默认仅免费快速收取。

## 已修复问题

| 问题 | 原因 | 修复 |
| --- | --- | --- |
| 区服网关返回空对象 | 账号服返回的 `zid` 是数值，脚本曾转换成字符串后再转发。 | 网关请求保留原始数值型 `zid`。 |
| 加密游戏报文被拒绝 | 原生 WebSocket 桥接可能把 Pack1 Base64 文本交付为二进制 `Uint8Array`。 | 收包同时支持文本帧和二进制帧的 Base64 数据。 |
| HTTP 响应泄露到终端 | 调试残留直接输出原始 HTTP 响应。 | 已删除该输出，脚本不打印 token 或会话内容。 |

## 实际执行结果

已在区服 `S1`（`4101`）完成一次庄园收获，服务端返回成功，并收到以下材料变动：

| 物品 ID | 本次增加 |
| --- | ---: |
| `11` | `+9828` |
| `12` | `+6552` |
| `13` | `+1092` |
| `50` | `+8400` |

## 文件与验证

- `harvest_fief.py`：可执行的庄园收获客户端。
- `庄园收获接口分析.md`：消息、封包、响应和源码位置分析。
- `活动签到接口分析.md`：复用的 WebSocket 登录与 Pack1 协议分析。
- 已执行 `../.venv/bin/python -m py_compile harvest_fief.py`。
- 已执行 `../.venv/bin/python harvest_fief.py --self-test`，DES 标准向量、Pack1、Protobuf 和模拟完整收获会话均通过。

## 使用方式

```bash
../.venv/bin/python harvest_fief.py
../.venv/bin/python harvest_fief.py --mode free
../.venv/bin/python harvest_fief.py --mode pay
```

指定区服：

```bash
../.venv/bin/python harvest_fief.py --zone-id ZONE_ID --mode free
```

仅执行本地协议自检：

```bash
../.venv/bin/python harvest_fief.py --self-test
```

## 当前待办

- 如需在真实区服留存快速收取（`mode=free`）的成功 JSONL 记录，可在有免费次数时执行 `--mode free` 并对照游戏界面。
- 后续如需将材料 ID 显示为游戏内名称，可继续解析本地物品配置表并建立 ID 到名称的映射。
