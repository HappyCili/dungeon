# 游戏入口解析

更新时间：2026-07-19

## 用途

本文只定义“进入游戏服”这一段可复用链路：从本地 `tokens.json` 的游戏侧凭据，到账号服、区服网关、游戏服 WebSocket 登录完成。它不包含庄园征收或其他业务消息，也不保存令牌、会话密码或动态 WebSocket 地址。

## 已解析的入口数据契约

入口由两次 HTTP 请求和一次 WebSocket 登录组成：

```text
tokens.json
  userid + verify_token
    -> 账号服 Logincheck
      -> data.token + data.gate_url + data.zoneinfo
        -> 选择 zone.zid
          -> 区服网关 { zid, token }
            -> 顶层条目[*].servers[0].url
              -> 游戏服 WebSocket Login
                -> Pack_password
                  -> Login_reunique（业务消息可发送）
```

入口解析成功后可抽象为以下不含敏感值的对象：

```text
GameEndpoint
  url:       区服网关返回的 servers[0].url
  game_token: Logincheck.data.token
  zone_id:   已选 zone.zid
  zone_name: 已选 zone.zname
```

重要区分：`tokens.json` 中的 `verify_token` 只用于构造账号服 `Logincheck` 的客户端信息；游戏服首包 `Login` 使用的是账号服返回的临时 `data.token`，两者不是同一个字段。

## 原始客户端代码流程

### 1. 账号服令牌与区服列表

`decrypted-js/main.js:442-460` 的 `AccountServer` 保存并处理入口状态：

1. `requestToken()` 组织 `cmd: "Logincheck"`、时间戳、校验值、`b_str` 和渠道扩展信息。
2. `tryRequestToken()` 将请求发送到 `acc_url`。
3. `processToken()` 在 `ret == 0` 时读取 `data.token`、`data.gate_url`、`data.zoneinfo`，随后调用 `selectDefaultZone()`，再调用 `login()`。
4. `login()` 在已有 `token` 和 `gate_url` 时调用 `ServerManager.requestList(gate_url, token, selectedZone)`。

`AccountServer.selectDefaultZone()` 位于 `decrypted-js/main.js:466`。选择顺序是：可验证的 `Sps` 项、上次保存的区服、`Historys[0]`、`Recos[0]`、最后从 `Sps` 中随机选择一项。显式指定区服时应使用该区服对象的原始 `zid` 值。

### 2. 区服网关解析游戏服地址

`ServerManager.requestList()` 位于 `decrypted-js/main.js:7612-7613`：

```text
POST gate_url
body = { zid: selectedZone.zid, token: AccountServer.token }
```

客户端不会把 `zid` 强制转换为字符串；需要保留账号服区服对象中的原始数值或字符串类型。

`ServerManager.requestListResponse()` 位于 `decrypted-js/main.js:7621`。它按如下方式解释网关原始响应：

```text
for each top-level key K in response:
  zone = response[K]
  server = zone.servers[0]
  entry = {
    sortIdx: Number(K),
    id: zone.z_id,
    index: zone.z_index,
    name: zone.z_name,
    type: zone.z_type,
    load: server.load,
    url: server.url
  }
```

只有存在非空 `servers` 数组且首项含 `url` 的顶层条目才会成为游戏入口。客户端随后取第一个解析出的 `entry`，调用 `connectServer(entry)`；`connect()` 位于 `decrypted-js/main.js:7626`，并把 `selected.url` 传给 `SocketManager.instance.connect(url)`。

### 3. 游戏服 WebSocket 登录

`decrypted-js/main.js:6648` 组织未加密的首个 `Login` 消息：

```text
{
  pid: 1,
  startupTs: 0,
  token: AccountServer.token,
  unique: <运行时 unique>,
  lastSId: <运行时 lastReceiveId>
}
```

`decrypted-js/main.js:8139-8140` 的 `NativeWebSocketImpl` 使用 `TJ.TJWebSocket(url)` 建连；文本数据走 `SendText`。收到 `Pack_password` 后保存会话密码，收到 `Login_reunique` 后入口阶段完成。封包、解包和消息分发位于 `SocketManager`（模块 `23307`）；详细 Pack1 格式见 [活动签到接口分析.md](活动签到接口分析.md)。

## Python 对应流程

`harvest_fief.py` 已将入口解析集中在以下可复用位置：

| 目的 | Python 位置 | 对应客户端阶段 |
| --- | --- | --- |
| 读取最小令牌集 | `load_tokens()`，755-767 行 | 从本地凭据取得 `userid`、`verify_token` |
| 构造 HTTP 请求 | `_post_body()`，770-784 行；`post_json()`，787-814 行 | 账号服和网关调用 |
| 选择区服 | `_choose_zone()`，825-852 行 | `AccountServer.selectDefaultZone()` |
| 提取 WebSocket 地址 | `_extract_game_url()`，855-868 行 | `ServerManager.requestListResponse()` |
| 解析完整入口 | `resolve_game_endpoint()`，932-985 行 | `Logincheck -> gate_url -> servers[0].url` |
| 持有入口结果 | `GameEndpoint`，924-929 行 | `ServerManager.selected` |
| 执行 WebSocket 登录 | `FiefClient.harvest()`，1214-1238 行 | `SocketManager.connect -> Login -> Pack_password -> Login_reunique` |

后续功能只应复用 `resolve_game_endpoint()` 返回的 `GameEndpoint`：其中 `url` 用于建立游戏服连接，`game_token` 用于 `Login`。业务消息不应重新使用 `verify_token`，也不应跳过入口握手。

## 当前报错的准确位置

`庄园收获失败：区服网关没有返回游戏 WebSocket 地址` 由 `harvest_fief.py:855-868` 的 `_extract_game_url()` 抛出。它与庄园消息 `19154` 无关，表示脚本拿到的网关 JSON 未匹配客户端所需的结构：

```json
{
  "<任意顶层键>": {
    "z_id": "...",
    "z_index": "...",
    "z_name": "...",
    "z_type": "...",
    "servers": [
      {
        "url": "wss://...",
        "load": 0
      }
    ]
  }
}
```

该错误的定位顺序应固定为：

1. 确认账号服成功响应中存在 `data.token`、`data.gate_url` 和 `data.zoneinfo`。
2. 确认选中的 `zone.zid` 未被转换类型，且 `zone.zopen` 不是 `0`。
3. 对照 `main.js:7612-7621` 检查网关响应是否为顶层区服映射，而不是额外包了一层 `data`、错误对象或空对象。
4. 仅在网关响应符合上述契约后，提取 `servers[0].url` 并进入 WebSocket 登录阶段。

当前目录没有写入实际 WebSocket 地址的入口文件，因为现有网关响应未产生符合客户端契约的 `servers[0].url`。入口的静态流程与复用锚点已经完整保存在本文；动态地址应由每次有效的区服网关响应即时解析，不应长期持久化。

## 复用边界

- 新增游戏服功能时，复用 `GameEndpoint`、WebSocket 建连和登录握手；业务模块只实现各自的 Protobuf 请求与响应解析。
- 需要排查入口时，只检查账号服响应、所选区服对象和网关原始响应的字段形状，不进入业务消息发送流程。
- `tokens.json` 为本地凭据文件，本文不复制其内容；当前文件权限为 `0600`。
